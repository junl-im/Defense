# PROJECT HANDOFF — CURRENT v9.0.0

- Project: `DokkaebiLuckDefense3D_FULL_v9.0.0`
- Baseline: user-provided `Defense.zip` v8.0.0
- Game version: `9.0.0`
- Engine version: `7.0.0`
- Save schema: `7`
- Absolute art lock: `DD-ABSOLUTE-ART-BIBLE-2.0`
- Production-approved assets: `0`

## Asset Renaissance diagnosis

The imported v8 library contains 970 raster source files, not 970 finished assets. v9 separates them into:

- 40 named 512px high-resolution review candidates
- 101 manually selected sheet crops for reference only
- 823 auto-sliced low-resolution fragments in quarantine
- 6 source atlases
- 0 transparent files
- 0 production-approved deliverables

## v9.0.0 implemented

- Deterministic quality registry for all 970 files.
- Honest source-file, review-candidate and production-approved counts.
- Studio Asset Review OS v9 with tier, category, score and issue filters.
- Three high-resolution dokkaebi hero candidates routed into the title class selector as review art.
- Raw fragments forbidden at runtime and reference crops forbidden as final assets.
- Production Console reports 40 High-Res, 823 Quarantine and 0 Approved.
- Game 9.0.0, Engine 7.0.0 and asset cache revision 9.0.0.

## Non-negotiable status

No single-view PNG may be described as a final character model. The 19 runtime GLBs remain legacy candidates. The 1,130-asset mass-production plan stays locked until the six golden vertical-slice categories pass all Absolute Art Bible evidence gates.

---

# PROJECT HANDOFF — CURRENT v8.0.0

- Project: DokkaebiLuckDefense3D_FULL_v8.0.0
- Raster IP library: 970
- Curated candidates: 141
- Raw concepts: 823
- SVG: 0
- Concept generation: UNLOCKED
- Production approved: 0

---

# PROJECT HANDOFF — CURRENT v7.0.0

- Project folder: `DokkaebiLuckDefense3D_FULL_v7.0.0`
- Game version: `7.0.0`
- Engine version: `6.0.0`
- Save schema: `7`
- Absolute art lock: `DD-ABSOLUTE-ART-BIBLE-2.0`
- Production-approved runtime assets: `0`

## v7.0.0 implemented

- Ten elemental reactions integrated into the actual damage path.
- Mythic Momentum gauge and 7.5 second Overdrive integrated into damage and rewards.
- Boss escalation based on HP, phase and encounter time.
- Player-facing momentum meter and F4 production diagnostics.
- Status Effect System 2.0 and Combat Telemetry 2.0.
- Save migration and backup schema v7.
- Engine 6.0.0 runtime budgets extended for reaction and boss state diagnostics.

## Non-negotiable art status

The existing 19 GLBs are still `legacy-runtime-candidate`. Do not call them final or production approved. The 1,130-asset production list remains locked until hero, normal enemy, boss, environment, HUD and VFX all pass the golden vertical-slice gate.

---

# PROJECT HANDOFF — 도깨비 운빨 수호대 3D

## 2026-07-23 — ABSOLUTE ART BIBLE v2.0 LOCK

- 최상위 아트 잠금: `DD-ABSOLUTE-ART-BIBLE-2.0`
- 원문: `docs/ABSOLUTE_ART_BIBLE_v2.0.md`
- 기계 규격: `docs/ART_BIBLE_MACHINE_SPEC_v2.0.json`
- 검수표: `docs/ART_REVIEW_CHECKLIST_v2.0.md`
- 기존 42/35/23, 9개 애니메이션, Rainbow 희귀도 표현은 폐기했다.
- 신규 제작 계약은 42/18/15/25, 눈 28%, 최대 4색·60/25/10/5, 무지개 금지, 11개 필수 클립, 교체 장비 5파츠다.
- 현재 런타임 GLB 19종 중 Absolute v2.0 최종 통과 에셋은 **0종**이다. 기존 8종 기술 리뷰와 11종 프로토타입은 레거시 후보로만 유지한다.
- CHARACTER DNA v3.0은 확장 초안이며 v2.0 절대 규칙을 변경할 수 없다.


이 문서는 대화가 끊기거나 다른 작업자가 이어받아도 프로젝트를 계속 개발할 수 있도록 모든 핵심 기록을 누적하는 단일 인수인계 파일입니다.

- 마지막 갱신: 2026-07-23
- 현재 버전: `6.0.0`
- 프로젝트 폴더: `DokkaebiLuckDefense3D_FULL_v6.0.0`
- 현재 패치명: Battlefront Ascension 초대규모 통합 업데이트

---


## v6.0.0 신규 구현 요약 — Battlefront Ascension

- 게임 `6.0.0`, 엔진 `5.0.0`, 세이브 스키마 `6`
- Encounter Director 1.0: 표준·군집·철갑·폭풍·주술·월식·보스 공세 교리
- 신목 체력과 직전 웨이브 클리어 시간을 이용한 제한적 적응 난이도
- 웨이브별 적 구성·개체 수·생성 간격·체력·속도·공격력·정예 확률·보상 배율 통합
- 연소·빙결·풍인·균열·공명·감전 6종 상태이상과 중첩·지속·틱 피해
- Runtime Budget Manager: 품질 프로필·FPS·P95·위험 프레임 기반 활성 적 상한 조정
- Combat Telemetry: 피해원·상태이상·처치·보스·웨이브 클리어 기록
- 세이브 스키마 v6 적용 전 기존 주요 설정과 기록 자동 백업
- F4 Battlefront Production Console에 공세·전투·상태·예산 카드 추가
- Battlefront Ascension 타이틀·HUD·제작 콘솔 디자인 토큰 적용
- 신규 검증 `verify-v600.mjs` 추가

아트 승인 상태는 변경하지 않았다. 런타임 시스템 완성도와 최종 제작 에셋 승인은 별도이며, Absolute Art Bible v2.0 최종 승인 에셋은 계속 0종이다.

---

## v5.0.0 신규 구현 요약 — Moonstone Genesis

- Character DNA v3.0을 초안에서 프로덕션 잠금으로 승격
- 얼굴 42%, 눈 28%, 42/18/15/25 비율 자동 검증
- 직업 DNA 5종과 희귀도 DNA 7단계
- 교체 장비 5파츠, 필수 소켓 6개, 공통 애니메이션 11개
- 1,130개 제작 마스터리스트 전체에 Art Bible v2.0·DNA·생산 잠금 메타데이터 적용
- Engine 4.0.0 적응형 품질 프로필 4단계
- 평균·P95·P99·프레임 지터·부드러움 점수 계측
- HUD·그림자·월드 청크 프레임 예산 스케줄러 적용
- 기존 GLB 19종에 런타임 재질 조화 처리를 적용하되 최종 승인 0종 유지
- Warm Key + Cool Blue Rim 전장 조명과 Gold Border + Blue Glow UI 리마스터
- F4 Moonstone Production Console 추가
- 신규 계약 검증 `verify-v500.mjs` 추가

런타임 조화 처리와 최종 제작 승인은 완전히 별개다. 골든 수직 슬라이스 6종의 최종 에셋은 아직 승인되지 않았으며, 1,130개 대량 생산 잠금도 유지한다.

---

## v4.2.0 신규 구현 요약

- Absolute Art Bible v2.0 기반 `art-production-gate.js` 추가
- 골든 수직 슬라이스 6개 영역: 도깨비 전사, 일반 요괴, 저승 호랑이, 전장, HUD, VFX
- 승인 증거 8종이 모두 없으면 `production-approved` 표기 금지
- 기존 전투 GLB 19종은 `legacy-runtime-candidate`로 유지하고 최종 승인 0종을 명시
- 골든 수직 슬라이스 6종 승인 전 1,130개 대량 제작 잠금
- Engine 3.1.0에서 240프레임 고정 텔레메트리 버퍼 추가
- 평균 프레임 시간, P95, 25ms 이상 장시간 프레임, 40ms 이상 위험 프레임 집계
- `?stats=1` 렌더 HUD와 성능 로그 JSON에 텔레메트리·아트 게이트 상태 포함
- `verify-v420.mjs`로 제작 잠금과 성능 계측 계약 자동 검증

신규 작가 제작 승인 모델 수는 여전히 0종이다. v4.2.0은 다음 골든 수직 슬라이스 제작을 시작하기 전에 승인 오염과 성능 회귀를 막는 기반 패치다.

---

## v4.1.0 신규 구현 요약

- 플레이어 공격 대상 선회와 0.18초 공격 방향 잠금
- 영웅 기술 대상 선회와 0.36초 방향 잠금
- 수호대 공격 전 예측 조준과 발사 순간 정렬
- 적의 신목 타격 전 방향 정렬
- `WeaponSocket` 기반 플레이어·수호대 발사 원점
- 전사·바람·화염·서리·바위·방울·번개별 임팩트 프리셋
- 치명타·보스·궁극기 히트스톱, 화면 플래시, 카메라 흔들림 계층
- 저사양 모바일 34개 / 일반 72개 동시 VFX 예산
- 보상 선택 완료 후 10초 자동 다음 웨이브
- 자동 진군 중 버튼 즉시 시작, 마지막 3초 강조, 일시정지 정지
- `combat-presentation.js`와 `verify-v410.mjs` 추가

작가 제작 승인 모델 수는 여전히 0종이다. v4.1.0은 기존 19종 모델의 전투 방향·소켓·VFX 활용을 강화한 런타임 표현 패치다.

---

## v4.0.0 신규 구현 요약

- 무기·부적·신발 3슬롯 영구 장비
- 장비 9종, 원정 종료 드롭, 중복 장비 정수 전환
- 전사·궁수·법사별 숙련 레벨과 영구 전투 보정
- 타이틀·HUD·일시정지 장비 가방 진입
- 도깨비마을 4구역 진행 표시와 상단 레일 충돌 감사
- 보스 공격 임박 화면 프레임
- 결과 화면 장비·숙련 보상 카드
- `verify-v400.mjs` 회귀 계약

현재 작가 제작 승인 모델 수는 변경하지 않았다. v4.0.0은 성장·UI·전투 정보 구조 패치이며 신규 모델을 완성 에셋으로 과장하지 않는다.

---

## 1. 사용자 요구와 산출물 규칙

사용자는 AAA급 사실성보다는 모바일에서 가볍게 돌아가면서도 첫인상과 성장 연출에서 감탄이 나오는 3D 웹게임을 원합니다.

선호 감성:

- 도깨비 키우기류의 수집과 성장
- 운빨 디펜스류의 랜덤 소환, 합성, 잭팟
- 플레이어가 3D 공간을 직접 돌아다니는 액션
- 짧은 판 안에서 크게 성장하는 피드백

반드시 유지할 전달 방식:

1. 매 패치마다 **전체 프로젝트 ZIP과 변경분 패치 ZIP 두 개만** 제공
2. 체크섬, 개별 문서, 파일 목록은 별도 다운로드로 제공하지 않음
3. `PROJECT_HANDOFF.md`를 매 패치 누적 갱신하고 두 ZIP 내부에 포함
4. 다운로드 파일은 답변 최하단 배치
5. 두 파일 아래에 다음 패치 예상 라인 표시
6. ZIP에 `node_modules`, `.firebase`, 로그 파일 제외
7. 전체 ZIP에는 미리 빌드된 `dist/` 포함
8. 패치 ZIP은 중간 폴더 없이 프로젝트 상대 경로의 변경 파일만 담음
9. 패치 ZIP에는 `dist/`와 별도 적용 안내 문서를 넣지 않음
10. 프로젝트 루트 Markdown은 `README.md`, `PROJECT_HANDOFF.md` 두 개만 유지

---

## 2. 기존 Defense 프로젝트에서 얻은 교훈

기존 `Defense.zip`은 기능 부족이 아니라 중심 재미를 검증하기 전에 에셋, UI, 패치, 폴백 시스템이 과도하게 누적된 것이 문제였습니다.

분석 당시 주요 상태:

- 전체 약 550MB
- node_modules 제외 약 249MB
- 이미지 약 3,300개
- 버전명이 붙은 에셋 약 2,885개
- 소스 약 37,700줄
- 대형 단일 씬 파일과 여러 미술 방향 공존

재사용한 개념:

- 웨이브와 적 데이터 구조
- 로컬 저장 우선 방식
- Firebase 익명 로그인과 랭킹
- 모바일 저사양 대응

버린 것:

- Phaser 렌더링과 기존 화면 구조
- 대량 이미지 에셋
- 복잡한 로그인, 월드맵, 상점, 성장 메뉴
- 여러 아트 스타일과 복구 시스템의 중복

개발 원칙:

> 메뉴와 콘텐츠 양보다 한 판의 코어 재미를 먼저 완성한다.

---

## 3. 게임 한 줄 정의

3D 달빛 야시장을 직접 뛰어다니며 엽전을 줍고, 랜덤 도깨비를 소환·합성해 사방에서 몰려오는 요괴를 막는 8~10분 액션 운빨 디펜스.

핵심 루프:

`이동 → 자동 공격 → 엽전 직접 수집 → 랜덤 소환 → 3개 자동 합성 → 집중 명령 → 인연 완성 → 축복 선택 → 보스 격파`

절대 흔들지 않을 요소:

- 플레이어가 직접 움직인다.
- 골드는 바닥에 떨어지고 직접 주워야 한다.
- 소환은 랜덤이며 나쁜 운은 대박 기운으로 누적된다.
- 동일 종류·동일 별 3개는 자동 합성된다.
- 짧은 전투 판의 밀도를 복잡한 메뉴보다 우선한다.

---

## 4. 기술 스택

- Three.js `0.185.1`
- Firebase Web SDK `12.16.0`
- Vite `8.1.5`
- Vanilla JavaScript ES modules
- HTML/CSS HUD
- Web Audio API 절차형 효과음
- PWA manifest + 구버전 서비스워커 제거용 일회성 복구 스크립트
- Firebase 익명 로그인 + Firestore 랭킹
- GitHub Actions + GitHub Pages

Node 권장:

- Node.js `20.19+` 또는 `22.12+`

명령:

```bash
npm install
npm run verify
npm run dev
npm run build
npm run preview
npm run deploy:rules
npm run deploy
```

---

## 5. 연결된 GitHub와 Firebase

### GitHub

기존 Defense 문서에서 확인한 Pages 주소:

- `https://junl-im.github.io/Defense/`

예상 저장소:

- `https://github.com/junl-im/Defense`

주의:

- 제공된 ZIP에는 `.git`이 없어 저장소 주소는 Pages 주소를 바탕으로 역추론함
- GitHub Desktop에 표시되는 실제 origin이 다르면 실제 origin을 우선
- Pages base path는 `/Defense/`
- 워크플로: `.github/workflows/deploy.yml`

### Firebase

기존 Defense 설정 파일에서 직접 확인:

- 프로젝트 ID: `web-game2`
- Auth Domain: `web-game2.firebaseapp.com`
- Storage Bucket: `web-game2.firebasestorage.app`
- 연결 파일: `.env.production`, `.firebaserc`

새 게임 랭킹 컬렉션:

- `dokkaebiScores`

기존 Defense에서 사용하는 Firestore 경로:

- `users`
- `leaderboards/{boardId}/scores/{uid}`
- `dailySeeds`

`firestore.rules`는 기존 경로와 `dokkaebiScores`를 모두 보존하는 병합 규칙입니다. 새 게임 규칙만 단독 배포하면 기존 Defense 기능이 깨질 수 있습니다.

권장 배포:

- 정적 게임: GitHub Pages
- 익명 로그인과 랭킹: Firebase
- Firebase Hosting은 기존 콘텐츠 교체 가능성이 있으므로 선택 사용

---

## 6. 현재 구현 상태

### 전장과 조작

- 달빛 야시장 단일 3D 전장
- 중앙 신목과 사방 요괴문
- 절차형 등불, 시장 좌판, 달, 바위, 도깨비불
- 모바일 조이스틱 이동
- 오른쪽 드래그 카메라
- PC 키보드 조작
- 플레이어 자동 기본 공격
- 질주
- 도깨비불 난무 광역기
- 적 처치 후 엽전 직접 수집

### 도깨비 6종

- 불씨 깨비: 연속 공격
- 달서리 깨비: 둔화
- 바람 갓깨비: 장거리 관통
- 바위 몽둥깨비: 범위 공격
- 방울 무당깨비: 연쇄 공격
- 번개 장군깨비: 처형

### 성장과 운빨

- 1성~5성
- 동일 종류·동일 별 3개 자동 합성
- 별에 따라 크기, 뿔, 오라, 발광, 투사체 강화
- 슬롯 15개
- 슬롯이 가득 차면 가장 약한 도깨비 환생
- 랜덤 소환
- 낮은 등급 소환 시 대박 기운 증가
- 대박 기운 100%에서 영웅 이상 보장
- 인연 시너지 6종
- 달의 축복 9종

### 적과 웨이브

- 일반 적 4종
- 보스 2종
- 총 10웨이브
- 웨이브 5: 저승 호랑이
- 웨이브 10: 백귀 야행왕

### 전투 피드백

- 첫 2성 도깨비 무료 강림
- 첫 웨이브 자동 시작
- 미션 시네마틱 배너
- 보스 전용 체력바
- 연속 처치 체인
- 10연속 처치 엽전 보너스
- 치명타와 3D 전투 숫자
- 무결점 웨이브 보너스
- 모바일 진동
- 평균 FPS 기반 동적 해상도 조절

### v1.3 궁극 진화와 초행 임무

- 5성 도깨비 6종 고유 자동 궁극기
- 5성 탄생 시 약 2.25초 카메라 집중 연출
- 전용 신화 각성 배너, 광주, 파티클, 진동
- 보유 목록에 합성 `1/3`, `2/3` 진행 표시
- 첫 플레이 전용 임무 3단계
- 임무 완료 상태를 로컬 저장하여 이후 판에서는 숨김
- 게임 데이터와 SoundEngine 최소 단위 모듈 분리

---

## 7. v1.2.0 변경 기록

목표:

- 중앙으로 걷기만 하던 적에게 피하고 유도하고 측면을 잡는 판단을 추가
- 랜덤 소환의 재미를 유지하면서 제한된 선택권 제공
- 프로젝트 문서와 패치 파일 누적을 정리

구현 내용:

### 두억 질주꾼

- 일정 거리에서 이동 정지
- 0.82초 동안 붉은 돌진 경로 표시
- 신목 방향으로 고속 돌진
- 플레이어와 충돌하면 밀쳐내고 짧은 혼절
- 신목에 도달하면 기본 공격보다 큰 피해

### 저주 무당

- 플레이어 현재 위치에 보랏빛 장판 예고
- 예고 중 플레이어가 수호대에서 멀어져 장판을 외곽으로 유도 가능
- 활성 장판 안 도깨비 공격 주기 1.85배 증가
- 장판 지속 5.2초

### 돌갑옷 귀수

- 전방에 실제 3D 방패 모델 추가
- 방패 정면에서 들어오는 투사체 피해 65% 감소
- 방패 피격 시 발광과 `방패!` 전투 텍스트
- 플레이어가 측면 또는 후방으로 이동하도록 유도

### 보스 충격파

- 기존 즉발 포효를 1.08초 예고 링 공격으로 변경
- 범위 안 플레이어는 넉백, 1.05초 혼절, 궁극기 재사용 시간 +1.5초
- 범위 밖 회피 시 점수 +120

### 삼지선다 소환

- `삼지선다 부적` 축복 추가
- 웨이브 3 첫 축복 후보에 반드시 포함
- 선택 시 소환권 2개 획득
- 소환 등급은 기존 확률로 먼저 결정
- 같은 등급의 서로 다른 도깨비 3종 중 하나 선택
- 비용, 대박 기운, 자동 합성 구조는 기존 시스템 유지

### 파일 정리

- 루트 Markdown을 `README.md`, `PROJECT_HANDOFF.md` 두 개로 제한
- 이전 `GAME_DESIGN`, `PATCH_HISTORY`, `QA_CHECKLIST`, 패치 노트 등은 단일 인수인계 문서에 통합
- 패치 안내는 패치 ZIP의 TXT 파일로만 제공
- 검증 스크립트에서 루트 Markdown 개수를 검사

---

## 7-A. v1.3.0 변경 기록

목표:

- 5성을 단순 배수 상승이 아니라 한 판의 기억에 남는 사건으로 만들기
- 첫 플레이에서 소환과 합성 목표를 명확히 제시하기
- 큰 단일 파일의 정적 데이터와 오디오를 안전하게 분리하기

구현 내용:

### 5성 고유 궁극기

- 불씨 깨비 `적련 백화`: 목표 주변 반경 5.4 광역 화염
- 달서리 깨비 `월빙 결계`: 반경 10.5 피해와 4.8초 강력 둔화
- 바람 갓깨비 `천풍 만발`: 사거리 14.5 안 최대 12개 목표 공격
- 바위 몽둥깨비 `태산 붕괴`: 반경 4.8 고위력 피해와 일반 적 밀쳐내기
- 방울 무당깨비 `백귀 방울춤`: 사거리 13 안 최대 10연쇄
- 번개 장군깨비 `천뢰 심판`: 보스 우선 낙뢰, 일반 적 34% 이하 및 보스 12% 이하 처형
- 궁극기 재사용 시간은 종류별 13.5~18초

### 5성 탄생 연출

- 5성 직접 소환 또는 자동 합성 시 공통 실행
- 카메라가 해당 도깨비를 약 2.25초간 회전하며 집중
- 전투 시간은 연출 중 약 42% 속도로 감소
- 전용 배너에 도깨비 이름과 궁극기 이름 표시
- 다중 링, 신화색 파티클, 화면 흔들림, 진동과 전용 음향
- 연출 직후 첫 궁극기는 약 1.6초 뒤 사용 가능

### 합성 진행 표시

- 우측 도깨비 목록에 같은 종류·같은 별 보유 수를 `합성 1/3`, `합성 2/3`으로 표시
- 5성은 합성 진행 대신 `궁극 · 궁극기명` 표시

### 첫 플레이 임무

1. 도깨비 3회 강림: 35 엽전
2. 첫 자동 합성: 45 엽전
3. 저승 호랑이 격파: 80 엽전 + 삼지선다 선택권 1개

- 세 임무를 모두 완료하면 `dokkaebi-first-missions-complete` 로컬 키 저장
- 완료한 사용자의 이후 플레이에는 임무 패널을 표시하지 않음

### 모듈 분리

- `src/game-data.js`: 등급, 도깨비, 적, 시너지, 축복 데이터
- `src/sound-engine.js`: Web Audio 효과음 엔진
- `src/main.js`: 런타임 게임 로직과 Three.js 월드 유지

---

## 8. 주요 밸런스 값

파일: `src/main.js`

- 시작 골드: 70
- 첫 무료 소환: 2성 보장
- 기본 소환 비용: 30
- 4회 소환마다 비용 +5
- 최소 소환 비용: 18
- 최대 웨이브: 10
- 신목 체력: 100
- 플레이어 기본 사거리: 8.8
- 질주 재사용: 4.2초
- 광역기 재사용: 13초
- 체인 유지: 1.85초
- 무피해 웨이브 추가 보너스: `10 + wave × 2`

기본 소환 확률:

- 4성 약 1.8%
- 3성 약 8.7%
- 2성 약 18.5%
- 나머지 1성
- 대박 기운 100%: 3성 보장, 12% 확률로 4성

v1.2 패턴 값:

- 질주 경고: 0.82초
- 질주 속도: 11.5
- 질주 신목 피해 배율: 1.65
- 저주 예고: 0.82초
- 저주 지속: 5.2초
- 저주 공격 주기 배율: 1.85
- 방패 정면 피해 배율: 0.35
- 보스 충격파 예고: 1.08초
- 보스 충격파 반경: 5.5
- 삼지선다 소환권: 2회

v1.3 값:

- 신화 연출: 2.25초
- 연출 중 게임 시간 배율: 0.42
- 불씨 궁극 쿨타임: 13.5초
- 달서리 궁극 쿨타임: 16초
- 바람 궁극 쿨타임: 14.5초
- 바위 궁극 쿨타임: 17.5초
- 방울 궁극 쿨타임: 15초
- 번개 궁극 쿨타임: 18초
- 초행 임무 보상: 35 / 45 / 80 엽전
- 초행 최종 보상: 삼지선다 선택권 1개

실제 Android/iPhone 플레이 후 반드시 조정해야 합니다.

---

## 9. 파일 구조

```text
README.md
  실행, 조작, 배포를 위한 짧은 사용자 문서

PROJECT_HANDOFF.md
  모든 개발 기록, 연결 정보, 테스트, 다음 작업을 누적하는 단일 문서

index.html
  전체 게임 DOM, HUD, 모달, 선택 소환 화면

src/main.js
  Three.js 월드, 전투, 소환, 합성, 궁극기, 웨이브, 적 패턴, 보스 페이즈, 계약

src/game-data.js
  도깨비, 적, 등급, 시너지, 축복, 위험 계약 정적 데이터

src/sound-engine.js
  Web Audio 절차형 효과음 엔진

src/style.css
  모바일 HUD, 선택 소환, 신화 연출, 전투 피드백 스타일

src/firebase.js
  Firebase 익명 인증과 dokkaebiScores 랭킹

public/sw.js
  v1.1~v1.3의 구버전 캐시를 삭제하고 스스로 등록 해제하는 복구 스크립트

scripts/verify-project.mjs
  DOM, 버전, 문서 정리, 필수 기능 정적 검사

firestore.rules
  기존 Defense와 새 게임 랭킹 규칙 병합본

.github/workflows/deploy.yml
  main push 시 /Defense/ 경로 GitHub Pages 자동 배포
```

v1.3에서 정적 데이터와 오디오를 먼저 분리했습니다. `src/main.js`에는 Three.js 객체와 서로 강하게 결합된 런타임 로직이 남아 있습니다. 다음 분리 후보는 효과와 전투 텍스트이지만, 게임 기능 개발을 멈추는 대규모 리팩터링은 금지합니다.

---

## 10. 버전 기록

### v1.0.0

- 최초 3D 세로 슬라이스
- 플레이어 직접 이동과 골드 수집
- 도깨비 6종, 자동 합성, 인연 6종
- 적 4종, 보스 2종, 10웨이브
- 로컬/Firebase 랭킹과 PWA

### v1.1.0

- 첫 무료 2성 강림과 첫 웨이브 자동 시작
- 미션 배너, 보스 체력바
- 연속 처치, 치명타, 전투 숫자
- 무결점 보너스, 진동, 적응형 렌더 품질
- 기존 GitHub/Firebase 연결 반영

### v1.2.0

- 질주꾼 돌진 경고선과 충돌
- 저주 무당 장판 유도
- 돌갑옷 정면 방패
- 보스 충격파 예고 링과 회피 보너스
- 삼지선다 소환권
- 루트 문서 두 개로 정리

### v1.3.0

- 5성 도깨비 고유 궁극기 6종
- 5성 탄생 카메라와 신화 각성 연출
- 합성 `1/3`, `2/3` 진행 표시
- 첫 플레이 임무 3단계와 보상 저장
- `game-data.js`, `sound-engine.js` 최소 모듈 분리

### v1.4.0

- 구버전 서비스워커 캐시로 로딩 화면이 남는 문제 복구
- v1.1~v1.3 이전 진입 JS/CSS 임시 호환
- 빌드 진입 파일을 `assets/game.js`, `assets/game.css`로 고정
- 8초 부팅 감시와 캐시 정리 재시도 화면
- 저승 호랑이 2페이즈, 백귀 야행왕 3페이즈
- 보스 다음 공격 HUD와 위험 계약 3종
- 패치 ZIP을 저장소 루트 직접 덮어쓰기 구조로 변경

### v1.5.0

- 제목 화면 `updateUnits()`가 `this.mods` 생성 전에 실행되어 발생한 `Cannot read properties of undefined (reading 'unitCooldown')` 부팅 오류 수정
- `createDefaultMods()`로 전투 보정값 생성 경로 통합
- 제목 월드 생성 전에 기본 보정값을 선행 초기화
- `unitCooldown` 접근에 기본값 방어 추가
- 검증 스크립트에 초기화 순서 회귀 검사 추가
- 겹친 위험 중 우선순위가 가장 높은 하나만 회피 방향·발동 시간으로 표시
- 돌진, 저주, 도약, 충격파, 백귀 야행진 위험 안내
- 결과 화면 혼불 조각 보상
- 영구 성장 3종: 시작 엽전, 신목 체력, 도깨비 공격력
- 영구 성장 5단계와 손상된 로컬 저장 기본값 복구

---

## 11. v1.6 핵심 수치

집중 명령:

- 도깨비 목록 칩을 눌러 발동
- 같은 종류·같은 별 도깨비 전체 적용
- 지속시간 7초
- 공용 재사용 대기시간 18초
- 공격력 +55%
- 공격 주기 배율 0.62
- 5성 궁극 충전 속도 1.7배
- 발동 시 5성 궁극 남은 시간 4초 감소

혼불 보상:

- 기본 8
- 진행 웨이브당 +2.4
- 처치 18회당 +1
- 최고 별당 +2
- 승리 시 +20
- 최종 지급 범위 8~70

영구 성장:

- 달빛 주머니: 단계당 시작 엽전 +10, 비용 12/22/34/50/70
- 신목 결계: 단계당 신목 최대 체력 +7, 비용 12/22/34/50/70
- 깨비 맹약: 단계당 도깨비 공격력 +3.5%, 비용 15/25/38/56/78
- 각 특성 최대 5단계

위험 안내 안정화:

- 적과 장판에 고유 런타임 ID 사용
- 덜 급한 새 경고는 0.14초 유지 후 전환
- 기존 경고가 한 프레임 사라져도 0.2초 완충
- 즉시 위험·발동 0.34초 이하·점수 차 72 이상은 즉시 전환

---

## 12. 버전 기록 추가

### v1.7.0

- 오른쪽 도깨비 칩을 누르는 집중 명령 추가
- 같은 종류·같은 별 도깨비 전체 7초 강화
- 공격력·공격속도·5성 궁극 충전 강화
- PC `R` 키 최고 등급 자동 명령
- 위험 대상 고유 ID와 전환 지연으로 안내 흔들림 완화
- 제목 화면에 다음 판 성장 적용값 3종 표시
- 혼불 보상과 성장 비용 재조정
- 명령 상태를 준비·재충전·집중 시간으로 칩에 실시간 표시

---

## 13. 검증 상태

2026-07-20 v1.6 자동 검증 완료:

- 깨끗한 `npm ci` 통과, 취약점 0건
- `npm run verify` 통과
- 106개 DOM ID 연결 통과
- package/runtime version `1.6.0` 일치
- 집중 명령 7초·18초 밸런스 정적 검사 통과
- 위험 대상 고유 ID와 전환 지연 검사 통과
- 제목 성장 요약 UI 연결 통과
- JavaScript 문법 검사 통과
- Vite 상대 경로 프로덕션 빌드 통과
- GitHub Pages `/Defense/` base path 빌드 통과
- v1.5 전체본에 패치 파일 덮어쓰기 후 v1.6 소스와 파일 비교 일치
- 패치 적용본 Pages 빌드와 v1.6 Pages 빌드 비교 일치
- 루트 Markdown 두 개 유지
- 전체 ZIP에 `dist/` 포함, 두 ZIP에서 `node_modules` 제외

환경 제한:

- 제작 환경 Chromium에서 EGL/ANGLE WebGL 컨텍스트 생성 실패 가능
- 자동 3D 화면 캡처와 실제 터치 플레이 검수 불가

실제 기기 필수 확인:

1. `?fresh=1.6.0` 접속 후 타이틀 진입
2. 제목 화면 성장 요약값과 실제 시작값 일치
3. 오른쪽 도깨비 칩 터치가 카메라 드래그로 오인되지 않는지
4. 집중 명령 7초 동안 칩과 3D 도깨비 발광 표시
5. 명령 재충전 숫자와 18초 후 재사용
6. 합성 중 명령 버프가 상위 별로 이어지는지
7. 장판 다중 발생 시 위험 안내 문구·화살표 흔들림 감소
8. 작은 화면에서 넓어진 도깨비 목록이 HUD를 가리지 않는지
9. 혼불 지급량과 성장 비용 체감

---

## 14. 알려진 위험

1. 도깨비 목록을 버튼으로 변경했으므로 매우 작은 화면에서 오른쪽 HUD 폭 확인 필요
2. 집중 명령이 5성 궁극과 동시에 겹치면 저사양 기기 파티클 부하 확인 필요
3. 합성 직전 세 도깨비에 명령하면 합성된 상위 도깨비로 남은 시간이 이전되도록 구현했으나 실제 연출 확인 필요
4. 위험 안내 전환 지연이 너무 길거나 짧은지 실제 기기 튜닝 필요
5. Firebase Hosting 배포 시 기존 `web-game2` Hosting 콘텐츠 교체 가능
6. Firestore 규칙은 병합 규칙 유지 필요
7. 로컬 저장 삭제 시 혼불 성장도 초기화됨

---

## 15. 다음 패치 예상 — v1.7.0

가칭: **도깨비 지휘 확장 & 전투 결과 분석**

예상 우선순위:

1. v1.6 실제 모바일에서 명령 버튼 터치 영역과 프레임 확인
2. 집중 명령을 도깨비 종류별 고유 효과로 차별화
3. 결과 화면에 최고 피해 도깨비와 명령 기여도 표시
4. 웨이브별 피해·수집·회피 간단 통계
5. 저사양 파티클 예산 추가 조정

---

## 16. 다음 작업자가 시작할 순서

1. 이 파일 전체 읽기
2. v1.6 전체 ZIP 또는 v1.5 저장소에 v1.6 패치 ZIP 적용
3. `npm install`
4. `npm run verify`
5. `VITE_BASE_PATH=/Defense/ npm run build`
6. GitHub Desktop에서 main 브랜치 commit·push
7. GitHub Actions 성공 확인
8. `https://junl-im.github.io/Defense/?fresh=1.6.0` 접속
9. 타이틀 진입과 시작 성장 요약 확인
10. Android Chrome과 iPhone Safari에서 집중 명령·위험 안내 검수
11. 다음 전달 시 전체 ZIP과 패치 ZIP 두 개만 제공


## v1.7.0 입력 시스템 개편
- WASD/방향키를 카메라 투영 방향으로 계산하고 대각선 속도를 정규화.
- blur/visibilitychange에서 눌린 키와 조이스틱 상태 초기화.
- 전체 지형 입력 레이어에서 짧은 탭은 이동, 11px 이상 드래그는 카메라 회전.
- 화면 좌표를 y=0 지형 평면에 레이캐스팅하고 경기장 경계 및 신목/상점 장애물로 보정.
- 목적지 3D 마커와 X/Z 좌표 확인 UI 추가.
- 도깨비별 집중 명령 효과 차별화 및 결과 화면 전투 분석 추가.

## v1.7.0 검증 결과
- npm ci 및 정적 검증 통과.
- 108개 DOM 연결 확인.
- 기본 Vite 빌드와 GitHub Pages `/Defense/` 빌드 통과.
- v1.6 전체본에 패치 ZIP 내용을 직접 덮어쓴 결과가 v1.7 전체 소스와 일치.
- 패치 적용본의 기본 빌드 및 Pages 빌드가 전체본 빌드와 일치.
- 패치 ZIP은 저장소 루트에 바로 덮어쓰는 8개 변경 파일만 포함하며 dist/node_modules를 포함하지 않음.

---

## v1.7.1 — Three.js 모바일 엔진 1.0.0 기반

### 버전 정책 변경

앞으로 작은 패치마다 마이너 버전을 올리지 않는다.

- 게임 패치: `1.7.1 → 1.7.7 → 1.7.7 ...`
- 큰 기능 묶음이 완성된 경우에만 `1.8.0`
- 엔진 버전은 게임과 별도로 관리하며 현재 `1.0.0`

### 엔진 선택

현재 프로젝트와 에셋 생성 방식이 Three.js에 이미 맞춰져 있으므로 Three.js를 유지한다.

- Babylon.js 전환은 기존 전투·UI·Firebase·절차형 모델을 다시 작성해야 해 현재 이점보다 비용이 큼
- Godot 4 Web은 향후 네이티브 앱과 에디터 중심 개발로 전환할 때 별도 브랜치에서 검토
- 현재 목표인 작은 용량, 빠른 GitHub Pages 배포, 모바일 웹 최적화에는 Three.js가 적합

### 추가된 엔진 모듈

- `MobileGameEngine`: 기기 감지, WebGLRenderer, pixel ratio, 품질 단계
- `PerformanceMonitor`: 일정 시간 FPS 표본과 동적 해상도 복구
- `InstanceBatch`: 정적·동적 InstancedMesh 관리
- `BlobShadowSystem`: 실시간 그림자 대신 한 번에 그리는 가짜 그림자
- `ObjectPool`: 파티클 재사용
- `GeometryBudget`: 모델별 triangles 검사
- `TextureAtlas`: 외부 텍스처 팩 도입 시 아틀라스 프레임 적용
- `WorldChunkManager`: 향후 오픈월드 청크 가시성 관리 기반

### 현재 게임에 실제 적용한 최적화

1. 모바일과 데스크톱 모두 실시간 shadow map을 기본 비활성화했다.
2. 플레이어·도깨비·적의 그림자를 단일 InstancedMesh blob shadow로 대체했다.
3. 바위 28개를 한 번의 인스턴스 배치로 변경했다.
4. 등불의 기둥·팔·램프를 세 개의 인스턴스 배치로 변경했다.
5. 야시장 상점의 카운터·지붕·기둥·항아리를 인스턴스 배치로 변경했다.
6. 모바일 PointLight 수를 최대 4개로 제한했다.
7. 파티클 생성과 dispose 반복을 ObjectPool 재사용으로 변경했다.
8. 동적 pixel ratio 상한과 FPS 기반 품질 하향·회복을 엔진으로 이동했다.
9. 외부 모델을 도입하기 전부터 자동 폴리곤 예산 검사를 빌드 검증에 포함했다.
10. Firebase 호출은 게임 루프에서 하지 않고 결과 저장에서만 수행한다.

### 폴리곤 검증 결과

도깨비 최대 예산 300 triangles:

- 불씨 252
- 달서리 272
- 바람 276
- 바위 300
- 방울 284
- 번개 276

적 최대 예산 500 triangles:

- 일반 196
- 질주꾼 236
- 돌갑옷 244
- 저주 무당 228
- 보스 328

### 텍스처 아틀라스 상태

현재 캐릭터와 월드는 이미지 텍스처보다 절차형 지오메트리와 단색 재질을 사용한다. 따라서 현재 단계에서 임의의 아틀라스 이미지를 추가하면 메모리와 로딩만 증가한다.

- `TextureAtlas` 엔진 모듈은 준비됨
- Kenney/Quaternius 등 외부 팩을 도입할 때 한 장의 아틀라스로 변환
- 외부 팩 도입 시 모델 material 수와 UV frame을 함께 검사

### 검증 상태

- `npm ci` 통과
- npm 취약점 0건
- 기존 108개 DOM 연결 검사 통과
- 게임 버전 `1.7.1`, 엔진 버전 `1.0.0` 확인
- 엔진 모듈 존재·연결 검사 통과
- 도깨비 300 / 적 500 triangles 자동 검사 통과
- Vite 프로덕션 빌드 통과
- GitHub Pages `/Defense/` 빌드 필요

### 다음 엔진 패치 우선순위 — 게임 1.7.7 / 엔진 1.0.3

1. UnitPad 15개의 base/rune도 동적 InstancedMesh로 합치기
2. 투사체와 엽전 풀링
3. Renderer draw calls / triangles 실시간 개발 HUD
4. 월드 청크별 정적 배치 분리와 거리 기반 표시
5. 외부 low-poly 모델 임포터와 triangles 검증 파이프라인
6. 텍스처 아틀라스 빌드 스크립트
7. 실제 Android Chrome과 iPhone Safari FPS 기록

### 다음 작업자가 시작할 순서

1. `PROJECT_HANDOFF.md`의 마지막 v1.7.1 항목 확인
2. `npm ci`
3. `npm run verify`
4. `VITE_BASE_PATH=/Defense/ npm run build`
5. Android와 iPhone에서 타이틀·전투 진입 확인
6. 콘솔의 `game v1.7.1 / engine v1.0.0` 진단 확인
7. 전투 중 실시간 그림자가 꺼져 있고 blob shadow가 보이는지 확인
8. 상점·등불·바위 배치가 정상인지 확인
9. 다음 버전은 `1.7.7`, 엔진 개선이면 `1.0.2`
10. 사용자에게는 전체 ZIP과 패치 ZIP 두 개만 전달


---

## v1.7.7 — Three.js 모바일 엔진 1.0.3

### 실제 적용 내용

1. 투사체를 종류별 ObjectPool로 전환했다.
   - 일반 구체, 바위, 바람 투사체 풀 분리
   - 모바일 최대 56개, 데스크톱 최대 96개 예산
   - 풀 초과 시 투사체 시각 효과만 생략하고 피해 판정은 즉시 처리
   - 매 발사마다 geometry/material 생성과 dispose를 반복하지 않음

2. 엽전을 ObjectPool로 전환했다.
   - 모바일 최대 72개, 데스크톱 최대 120개
   - 풀 초과 보상은 기존 엽전에 합산해 재화 손실 방지
   - 실제 획득량을 결과 분석의 `coinsCollected`에 기록

3. UnitPad 15개를 InstancedMesh로 변경했다.
   - base 15개를 한 드로우콜로 통합
   - rune 15개를 동적 한 드로우콜로 통합
   - 점유/해제 시 개별 인스턴스 색상과 크기만 갱신

4. WorldChunkManager를 실제 거리 기반 가시성 시스템으로 확장했다.
   - 위치를 청크 키로 변환
   - 활성 청크와 표시 오브젝트 통계 제공
   - 외곽 바위를 청크별 InstancedMesh로 분리
   - 플레이어 이동 시 인접 청크만 표시

5. RenderStatsHUD를 추가했다.
   - `?stats=1` 또는 F3
   - FPS, 품질 배율, draw call, triangles
   - geometry/texture 수
   - 활성/전체 청크와 오브젝트 수
   - 투사체/엽전 풀 사용량

### 버전

- 게임: `1.7.7`
- 엔진: `1.0.2`

### 다음 패치 예상 — v1.7.7 / 엔진 1.0.3

1. 적 모델과 도깨비 모델 geometry/material 캐시
2. 적 사망 시 모델 전체 dispose 대신 모델 풀 반환
3. 원거리 LOD 또는 저사양 단순 모델 전환
4. 고정 timestep 전투 시뮬레이션 검토
5. 실제 Android/iPhone 통계 HUD 측정값 기반 예산 조정

### 다음 작업자가 시작할 순서

1. 이 파일의 마지막 v1.7.7 항목 확인
2. `npm ci`
3. `npm run verify`
4. `VITE_BASE_PATH=/Defense/ npm run build`
5. `https://junl-im.github.io/Defense/?stats=1&fresh=1.7.7` 확인
6. F3 통계 HUD에서 draw call, triangles, 풀 사용량 확인
7. 외곽 이동 시 CHUNK 활성 수 변화 확인
8. 소환 발판 점유 색상과 자동 합성 후 해제 색상 확인
9. 다음 버전은 게임 `1.7.7`, 엔진 `1.0.2`
10. 사용자에게는 전체 ZIP과 패치 ZIP 두 개만 전달


## v1.7.7 / 엔진 1.0.3

- 적 모델을 종류별 ObjectPool로 재사용합니다.
- 적 geometry를 타입별 캐시로 공유해 웨이브 생성 비용과 GPU 메모리 중복을 줄였습니다.
- 원거리 일반 적은 눈·뿔·장비를 숨기는 경량 LOD로 전환됩니다.
- 타이틀 복귀와 재시작 시 적 풀 자산이 dispose되지 않도록 전용 EnemyPoolRoot를 사용합니다.


### v1.7.7 / engine 1.0.3
- A/D 화면 좌우축을 camera.matrixWorld 기반으로 수정.
- 모바일 320px, 360px, 390px, 430px 및 낮은 landscape UI 안전 규칙 추가.

## v1.7.7 / Engine 1.0.6
- 터치 이동 목적지 좌표 문구 제거.
- SVG 전면 금지 검증 추가.
- icon/cover를 PNG/WebP로 교체.
- GLB/GLTF 및 PNG/WebP/KTX2 전용 AssetPipeline 추가.


## v1.7.7 / Engine 1.0.6
- 모바일 왼쪽 정보 UI 접기/펼치기와 localStorage 상태 유지.
- 두 손가락 간격이 줄면 줌인, 벌어지면 줌아웃.
- PC 휠 줌과 목표 거리 보간으로 급격한 카메라 점프 방지.
- 줌 범위 9.5~22, 카메라 중심은 플레이어에 고정.


---

## v1.7.7 / Engine 1.0.6 — 카메라·조작 설정과 충돌 보정

### 적용 내용

1. `dokkaebi-control-settings-v1` localStorage에 다음 값을 저장한다.
   - 카메라 회전 감도
   - 핀치 줌 감도
   - PC 휠 감도
   - 최소 줌 거리
   - 최대 줌 거리
   - 모바일 조작 손 배치
2. 타이틀과 일시정지 화면에서 카메라·조작 설정 모달을 연다.
3. `controls-left-handed` body 클래스로 조이스틱과 액션 도크를 서로 교체한다.
4. 신목, 야시장 상점, 외곽 요괴문을 `cameraObstacles`로 관리한다.
5. 카메라 목표 선분과 구조물 원기둥의 교차를 계산해 관통 직전에 카메라 거리를 줄인다.
6. 충돌 진입은 빠르게, 장애물 이탈 후 원래 줌 복귀는 느리게 보간해 화면 튐을 줄인다.
7. 핀치·휠 방향, 줌 한계, 카메라 장애물 축소를 `verify-camera-controls.mjs`로 검사한다.

### 검증 순서

```bash
npm ci
npm run verify
VITE_BASE_PATH=/Defense/ npm run build
```

실기기에서 확인할 항목:

- 두 손가락을 모을 때 줌인, 벌릴 때 줌아웃
- 감도 60%와 160% 차이
- 오른쪽 이동 배치 선택 시 조이스틱이 우측, 액션 버튼이 좌측
- 신목과 상점 뒤로 카메라를 회전할 때 구조물 내부로 진입하지 않음
- 장애물에서 벗어난 뒤 카메라가 부드럽게 사용자 줌 거리로 복귀
- 320px 세로 및 낮은 가로 화면에서 설정 모달 넘침 없음

### 다음 패치 예상 — v1.7.8 / Engine 1.0.7

- 고품질 GLB 에셋 프리로드 매니페스트
- Draco/KTX2 로더 연결
- 에셋별 모바일/데스크톱 LOD 선택
- 로딩 진행률과 실패 에셋 대체 모델
- 실제 모바일 FPS 로그를 통계 HUD에서 내보내는 기능


## v1.7.7 / 엔진 1.0.6 완료
- 카메라 회전, 핀치, 휠 감도 사용자 설정 및 localStorage 저장
- 최소/최대 줌 범위 설정
- 신목/성문/상점 카메라 시야선 충돌 보정
- 모바일 왼손/오른손 조작 배치 전환
- 320px 세로 액션 독 2단 배치 및 100dvh 모달 overflow 방어
- 다음: v1.7.8 / 엔진 1.0.7 고품질 GLB 프리로드와 저사양 LOD 선택


---

## v1.7.8 / Engine 1.0.7 — 고품질 에셋 런타임 기반

### 적용 내용

1. `AssetPipeline`에 Three.js `GLTFLoader`, `DRACOLoader`, `KTX2Loader`를 연결했다.
2. 로더는 정적 메인 번들에 모두 넣지 않고 실제 GLB/KTX2가 카탈로그에 등록될 때 동적 import한다.
3. `CORE_ASSET_CATALOG`과 `MODEL_ASSET_SLOTS`를 추가했다.
   - 저사양: LOW
   - 일반 모바일: MEDIUM
   - 데스크톱: HIGH
   - 해당 변형이 없으면 인접 품질 변형으로 자동 폴백
4. 모델 로딩 실패 시 기존 절차형 플레이어·도깨비·적·환경 모델을 계속 사용할 수 있다.
5. 부팅을 비동기로 변경하고 로딩 화면에 다음 단계를 표시한다.
   - 렌더러 준비
   - 압축 로더 필요 여부 확인
   - 핵심 래스터 에셋 프리로드
   - 월드 생성
6. 기존 8초 부팅 감시를 18초로 늘려 저속 모바일에서 오탐 복구 화면이 뜨는 문제를 줄였다.
7. 텍스처 메모리 예산을 기기별로 분리했다.
   - 저사양 64MB
   - 일반 모바일 96MB
   - 데스크톱 192MB
8. `verify-texture-budget.mjs`가 PNG/WebP/JPEG/KTX2의 크기와 예상 GPU 메모리를 검사한다.
9. 현재 래스터 에셋 예상 GPU 메모리는 약 14.02MB이며 저사양 예산 64MB 안이다.
10. F3 통계 HUD에 에셋 품질 등급과 텍스처 사용량/예산을 추가했다.
11. 타이틀 화면에서 1600×900 WebP 키아트를 실제로 사용한다.
12. SVG 금지 정책은 그대로 유지한다.

### 중요한 현재 상태

- 실제 외부 캐릭터·환경 GLB는 아직 프로젝트에 포함하지 않았다.
- 품질과 라이선스가 확인되지 않은 임시 3D 모델을 넣지 않는 것이 사용자 요구에 더 안전하다.
- 현재 절차형 모델은 안전한 폴백이며, 다음 패치부터 검증된 고품질 GLB를 슬롯 단위로 교체한다.
- Draco/KTX2 디코더는 지원되지만 현재 카탈로그에 압축 에셋이 없으므로 첫 부팅 때 다운로드하지 않는다.

### 검증

```bash
npm ci
npm run verify
VITE_BASE_PATH=/Defense/ npm run build
```

검증 항목:

- 125개 DOM 연결
- 게임 `1.7.8`, 엔진 `1.0.7`
- GLB/Draco/KTX2 지연 로더
- 기기별 LOW/MEDIUM/HIGH 선택
- 비동기 부팅 오류 복구
- 텍스처 2048px 최대 변 제한
- 래스터 GPU 예상 메모리 14.02MB / 64MB
- 도깨비 300 / 적 500 triangles
- 카메라·WASD·320px 모바일 레이아웃
- GitHub Pages `/Defense/` 빌드

### 다음 패치 예상 — v1.7.9 / Engine 1.0.8

1. 첫 실제 고품질 환경 GLB 슬롯 도입
2. 환경 모델의 HIGH/MEDIUM/LOW 3단 LOD 연결
3. 로딩 실패·느린 네트워크 시 에셋별 타임아웃과 재시도
4. GLB material 수, draw call, triangles 자동 검사
5. 에셋 라이선스 출처를 프로젝트 내부 JSON에 기록
6. 모바일 통계 HUD 측정값 내보내기

### 다음 작업자가 시작할 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. `?stats=1&fresh=1.7.8`로 접속
5. 로딩 바가 0→100%로 움직이고 타이틀이 열리는지 확인
6. F3에서 `ASSET LOW/MEDIUM/HIGH`와 텍스처 예산 확인
7. 네트워크 탭에서 GLB/KTX2가 없을 때 Draco/Basis 디코더가 요청되지 않는지 확인
8. 다음 버전은 게임 `1.7.9`, 엔진 `1.0.8`
9. 사용자에게는 전체 ZIP과 패치 ZIP 두 개만 전달


## v1.7.9 / Engine 1.0.8 — 캐릭터 애니메이션 상태 시스템

- `src/engine/animation-state-system.js` 추가
- GLB 클립 이름을 idle/move/attack/hit/death 상태에 자동 매칭
- 실제 `THREE.AnimationMixer`와 절차형 fallback을 동일 컨트롤러에서 처리
- 도깨비 공격, 적 이동·공격·피격·사망 상태 연결
- 모바일/저사양은 카메라 거리 밖 애니메이션 업데이트 중지 및 2프레임 간격 처리
- 적 모델 풀 반환 후 geometry/material을 dispose하던 수명 오류 수정
- 다음 예상: v1.7.10 / Engine 1.0.9 — 실제 GLB 에셋 슬롯 매니페스트와 애니메이션 이벤트(타격 프레임/발사 프레임)

---

## v1.8.0 / Engine 1.1.0 — BOOT RECOVERY 복구와 대규모 런 변주 업데이트

### 사용자가 보고한 첫 실행 오류

표시 문구:

```text
BOOT RECOVERY
달빛 장터를 깨우지 못했습니다
초기화 오류: Cannot read properties of undefined (reading 'group')
```

실제 원인은 `updateBlobShadows()`의 다음 조건이었다.

```js
this.player?.group?.visible !== false
```

플레이어가 아직 생성되지 않은 첫 프레임에는 위 표현식이 `undefined !== false`, 즉 `true`가 된다. 그 결과 바로 뒤에서 `this.player.group.position`을 읽으며 `group` 오류가 발생했다.

수정 원칙:

1. blob shadow 갱신은 `worldReady`가 참일 때만 실행한다.
2. 플레이어·도깨비·적마다 실제 `group` 존재 여부를 먼저 확인한다.
3. 월드 생성 전과 `clearWorld()` 시작 시 `worldReady = false`로 둔다.
4. 플레이어, 도깨비, 적, 월드 청크가 모두 준비된 뒤에만 `worldReady = true`로 전환한다.

이 오류는 happy-dom 기반 실제 부팅 하네스에서 기존 코드로 재현한 뒤 수정본에서 `BOOT_OK true`를 확인했다. 이어서 `startRun()`, `startWave()`, 적 생성과 전투 update 루프까지 스모크 테스트했다.

### 함께 수정한 잠복 오류

1. 플레이어 자동 공격에서 존재하지 않는 `unit.animation`을 참조하던 코드를 `this.player.animation`으로 수정했다.
2. 적을 ObjectPool로 반환하면 좌표가 즉시 `-100`으로 초기화되므로, 반환 전에 `deathPosition`을 복제해 파티클과 엽전 생성에 사용한다.
3. 타이틀 복귀와 새 런 시작 때 플레이어·도깨비·적 애니메이션 컨트롤러를 제거해 오래된 mixer가 남지 않도록 했다.
4. `AssetPipeline` 기본 기준 경로를 `import.meta.env?.BASE_URL || './'`로 변경해 비 Vite 검증 환경에서도 안전하게 동작시켰다.

### WebGL 부팅 내구성 강화

`MobileEngine` 렌더러 생성은 다음 순서로 최대 세 번 시도한다.

1. 기기 분석 결과에 맞는 기본 설정
2. antialias를 끈 호환 설정
3. low-power 저사양 설정

모든 시도가 실패할 때만 WebGL 지원 또는 하드웨어 가속을 확인하라는 명확한 오류를 던진다. 성공한 폴백 단계는 엔진 진단 정보의 `rendererFallback`에서 확인할 수 있다.

### 달의 징조 5종

새 모듈 `src/run-director.js`가 직전 징조와 겹치지 않도록 다음 웨이브 규칙을 선택한다.

- 풍요의 달: 보상과 행운 증가
- 붉은 달: 적 공격력과 처치 점수 증가
- 서리 달: 적 이동 속도 감소, 도깨비 재사용 대기시간 보정
- 천둥 달: 빠른 생성과 영웅 공격 강화
- 검은 월식: 적 체력·정예 확률·보상이 함께 증가

징조는 체력, 속도, 공격력, 보상, 점수, 생성 간격, 도깨비 쿨다운, 영웅 피해, 행운, 정예 확률 배율을 조합한다. 현재 징조와 핵심 효과는 전투 HUD에 표시한다.

### 정예 요괴 4종

3웨이브부터 일정 확률로 일반 적이 정예 속성을 얻는다.

- 질풍: 이동 속도 강화
- 철벽: 체력 강화
- 폭주: 공격력 강화
- 황금: 보상과 점수 강화

정예 요괴는 재사용 가능한 링 오라로 구분하며, 처치 수는 결과 분석에 기록한다. 별도 고폴리곤 모델을 만들지 않아 기존 적 500 triangle 예산을 유지한다.

### 무결점 웨이브와 달빛 방패

웨이브 동안 신목이 피해를 받지 않으면 달빛 방패를 1개 얻고 최대 3개까지 보관한다. 다음 피해 한 번을 완전히 막으며, 방패 방어 횟수는 런 통계에 남는다.

### 왕대박 폭주

- 4성 도깨비 소환: 8초
- 5성 도깨비 소환: 12초

폭주 중에는 플레이어와 도깨비의 공격 성능이 상승한다. 남은 시간은 별도 HUD로 표시하고 발동 횟수는 결과 분석에 추가한다.

### UI와 결과 분석

추가 DOM:

- `moon-omen`
- `moon-omen-icon`
- `moon-omen-name`
- `moon-omen-effect`
- `moon-ward`
- `moon-ward-value`
- `jackpot-rush`
- `jackpot-time`

추가 결과 통계:

- 정예 요괴 처치
- 달빛 방패 방어
- 왕대박 폭주 발동

### 변경 파일

- `package.json`
- `package-lock.json`
- `index.html`
- `README.md`
- `PROJECT_HANDOFF.md`
- `src/main.js`
- `src/style.css`
- `src/run-director.js`
- `src/engine/mobile-engine.js`
- `src/engine/asset-pipeline.js`
- `src/engine/animation-state-system.js`
- `src/engine/engine-config.js`
- `scripts/verify-project.mjs`
- `scripts/verify-run-director.mjs`

### 자동 검증 결과

```bash
npm ci
npm run verify
VITE_BASE_PATH=/Defense/ npm run build
```

확인 항목:

- DOM ID 133개 연결
- 게임 `1.8.0`, 엔진 `1.1.0`
- 첫 프레임 `player.group` 안전성
- 플레이어 공격 애니메이션 참조
- 적 풀 반환 전 사망 좌표 보존
- 달의 징조 5종과 정예 속성 4종
- 달빛 방패와 왕대박 폭주
- WebGL 렌더러 3단 폴백
- 애니메이션 컨트롤러 수명 정리
- 도깨비 300 / 적 500 triangle 예산
- 텍스처 메모리 14.02MB / 저사양 예산 64MB
- 카메라, 입력 방향, 320px 모바일 레이아웃
- 실제 부팅 및 첫 웨이브 스모크 테스트

### 실기기 확인 순서

1. 기존 사이트 캐시를 한 번 정리하고 `?fresh=1.8.0`으로 접속한다.
2. BOOT RECOVERY 화면 없이 타이틀이 열리는지 확인한다.
3. 첫 웨이브 시작 시 달의 징조 HUD가 표시되는지 확인한다.
4. 3웨이브 이후 오라가 있는 정예 요괴가 등장하는지 확인한다.
5. 신목 무피해 클리어 후 달빛 방패가 쌓이고 다음 피해를 막는지 확인한다.
6. 4성 또는 5성 소환 때 왕대박 폭주 타이머가 표시되는지 확인한다.
7. F3에서 `rendererFallback`, draw call, triangles, 풀 사용량을 확인한다.
8. 하드웨어 가속을 끈 환경에서는 호환 렌더러 재시도 또는 명확한 WebGL 오류가 표시되는지 확인한다.

### 다음 패치 예상 — v1.8.1 / Engine 1.1.1

1. 달의 징조별 전장 조명·안개·파티클 테마
2. 정예 속성별 실제 공격 패턴과 사망 보상 연출
3. 보스 전용 월식 페이즈와 웨이브 중간 선택 이벤트
4. 실제 고품질 환경 GLB 첫 슬롯과 HIGH/MEDIUM/LOW LOD
5. 모바일 실기기 성능 로그 내보내기와 자동 품질 강등 기록
6. 런 시드 저장과 동일 조건 재도전

### 다음 작업자가 시작할 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. `?stats=1&fresh=1.8.0`으로 접속
5. 부팅, 첫 웨이브, 징조 HUD, 정예 오라, 방패, 폭주를 순서대로 확인
6. 다음 버전은 게임 `1.8.1`, 엔진 `1.1.1`
7. 사용자에게 전체 ZIP과 패치 ZIP 두 개만 전달



---

## v1.9.0 / Engine 1.2.0 — 초대규모 원정·빌드 다양성 업데이트

### 패치 목표

v1.8.0에서 첫 실행 복구와 웨이브 변주 기반이 안정화되었다. 이번 버전은 단순 수치·적 종류 추가보다 매 판의 선택과 빌드가 달라지는 구조를 우선했다.

핵심 흐름은 다음과 같다.

`원정 난이도 선택 → 웨이브별 도전 → 혼불 충전 → 수호신 폭주 → 웨이브 후 유물·계약·축복 선택 → 결과와 영구 성장`

### 원정 난이도 3종

새 모듈 `src/expedition-director.js`의 `RUN_MODES`에서 관리한다.

1. 달빛 수호전
   - 기존과 같은 기본 난이도
   - 배율 1.0 기준
2. 월식 원정
   - 적 체력·속도·공격력 상승
   - 보상 1.18배, 점수 1.5배
   - 시작 엽전 +15, 정예 확률 +7%
3. 백귀 심연
   - 적 체력 1.55배, 공격력 1.35배
   - 보상 1.35배, 점수 2.1배
   - 시작 엽전 +25, 달빛 방패 +1, 정예 확률 +14%

선택은 `dokkaebi-run-mode-v1`에 저장한다. 타이틀 화면에서 각 모드의 적 체력, 점수, 정예 보정을 즉시 확인할 수 있다.

### 웨이브 도전과제 6종

매 웨이브 직전 직전 임무와 겹치지 않는 도전 하나를 생성한다.

- 결계 불변: 신목 무피해
- 정예 현상금: 정예 요괴 처치
- 백귀 연참: 목표 연속 처치
- 엽전 회수: 전투 중 직접 엽전 수집
- 귀화 폭격: 도깨비불 난무 누적 피해
- 질풍 순찰: 질주 사용

난이도와 웨이브에 따라 목표와 보상이 증가한다. 완료 시 엽전, 점수, 혼불을 함께 지급한다. 정예 현상금이 선택되면 웨이브 안에서 필요한 정예가 생성되도록 강제 보정해 불가능한 임무가 나오지 않게 했다.

무피해 도전은 신목이 손상되는 즉시 HUD를 실패 상태로 바꾸고 `결계 손상`을 표시한다.

### 원정 유물 12종

2·5·7·9웨이브 뒤에 세 가지 유물을 제시하며 최대 4개를 누적한다.

- 경제: 만월 복주머니, 야시장 장부
- 공격·공속: 도깨비 전고, 혼불 대도, 뇌신의 관
- 기동·스킬: 여우비 신발, 청염 화로
- 운과 소환: 삼재 대박부, 육성 성도, 심야 흥정패
- 방어: 신목 매듭
- 폭주: 메아리 방울

이미 선택한 유물은 다음 제시 풀에서 제외한다. 선택 셔플은 `Array.sort()` 난수 비교가 아니라 Fisher–Yates 방식으로 변경해 편향과 엔진별 결과 차이를 줄였다.

### 보상 선택 큐

같은 웨이브에 유물, 위험 계약, 축복이 동시에 예약될 수 있다. 기존처럼 하나의 모달이 다른 모달을 덮지 않도록 `postWaveQueue`를 추가했다.

- 유물 선택 완료
- 계약 선택 또는 건너뛰기
- 축복 선택
- 모든 선택이 끝난 뒤 다음 웨이브 버튼 활성화

각 단계는 상태를 다시 `playing`으로 돌린 뒤 큐의 다음 항목을 실행한다.

### 혼불과 수호신 폭주

혼불은 다음 행동으로 획득한다.

- 적 처치
- 엽전 직접 수집
- 무결점 웨이브
- 도전 진행과 완료
- 징조·난이도·영구 성장·유물 배율

100%에서 F키 또는 모바일 수호신 버튼으로 발동한다.

- 기본 지속 10초
- 대장 이동 속도 증가
- 대장 공격 피해 증가
- 수호대 공격 피해 증가
- 수호대 공격 주기 단축
- 메아리 방울로 지속 +4초와 추가 위력 보정

발동 횟수는 결과 통계에 남는다.

### 달의 징조 8종

기존 5종에 다음 3종을 추가했다.

- 사냥꾼의 달: 정예 확률과 정예 보상 강화
- 혼령의 달: 적 체력 상승, 수호대 공속과 혼불 획득 강화
- 새벽의 달: 빠른 적과 생성 간격 대신 대장 피해·보상 강화

징조의 `eliteReward`, `soulGain` 같은 선택 속성은 해당 시스템에서만 곱한다.

### 정예 속성 7종

기존 질풍·철벽·폭주·황금에 다음 속성을 추가했다.

- 결계: 최대 체력 비례 보호막
- 파열: 사망 시 위치를 보존해 경고 폭발과 기절 연출 생성
- 고대: 매우 높은 체력·공격력과 큰 보상

정예 외형은 기존 적 모델과 재사용 오라를 사용하므로 적 500 triangle 예산을 유지한다.

### 영구 성장 6종

기존 달빛 주머니, 신목 결계, 깨비 맹약에 다음 특성을 추가했다.

- 야행 보법: 이동 속도 단계당 +2.5%
- 운명 부적: 대박 기운 단계당 +5%
- 수호신 그릇: 시작 혼불 단계당 +8%

이전 저장 데이터에는 새 키가 없어도 `loadMetaProgress()`에서 자동으로 0레벨을 채운다.

### Engine 1.2.0 — 적응형 이펙트 예산

해상도 배율만 낮추던 기존 적응형 품질에 파티클 예산을 연결했다.

- LOW: 48%
- MEDIUM: 70%
- 모바일 기본: 82%
- 데스크톱 기본: 100%

`spawnParticles()`에서 요청 개수에 `effectBudgetScale`을 곱한다. 게임 규칙과 피해 판정은 바꾸지 않고 시각 이펙트 수만 줄인다. 품질 배지와 F3 진단 정보에도 현재 FX 비율을 표시한다.

### 모바일 UI

- 전투 버튼을 질주, 도깨비불, 수호신, 소환, 웨이브의 5버튼 구조로 확장
- 320px 세로 화면에서 조이스틱과 액션 독 사이 최소 5px 확보
- 웨이브 도전 HUD, 유물 스트립, 혼불 게이지 추가
- 430px 이하에서는 부가 설명과 일부 런 미리보기를 축약
- 가로 저높이 화면에서 타이틀과 전투 HUD 높이를 별도 보정
- 왼손 배치 규칙 유지

### 결과 분석 확장

- 선택한 원정 모드
- 완료한 도전 수
- 획득한 유물 수
- 수호신 폭주 발동 수
- 최대 연속 처치
- 질주 사용 수

혼불 영구 보상에도 원정 난이도, 도전, 유물 보너스를 반영한다. 로컬·온라인 랭킹 항목에는 `mode`를 추가한다.

### 변경 파일

- `package.json`
- `package-lock.json`
- `index.html`
- `README.md`
- `PROJECT_HANDOFF.md`
- `src/main.js`
- `src/style.css`
- `src/run-director.js`
- `src/expedition-director.js`
- `src/engine/engine-config.js`
- `src/engine/mobile-engine.js`
- `scripts/verify-project.mjs`
- `scripts/verify-run-director.mjs`
- `scripts/verify-expedition-system.mjs`
- `scripts/verify-mobile-layout.mjs`

### 검증 결과

실행 명령:

```bash
npm ci
npm run verify
VITE_BASE_PATH=/Defense/ npm run build
```

통과 항목:

- npm 의존성 취약점 0건
- DOM ID 연결 150개
- 게임 `1.9.0`, 엔진 `1.2.0`
- 원정 난이도 3종
- 유물 12종과 실제 적용 함수
- 웨이브 도전 6종, 목표·보상·진행 계산
- 징조 8종, 정예 속성 7종
- 강제 정예와 초기 웨이브 제한
- 영구 성장 6종
- 수호신 폭주와 혼불 HUD
- 유물·계약·축복 보상 큐
- FPS 연동 파티클 예산
- 320·350·360·390·410px 세로 모바일 배치
- 568px 가로 모바일 배치
- 카메라, 입력 방향, 애니메이션 상태
- 도깨비 300 / 적 500 triangle 예산
- 래스터 텍스처 메모리 14.02MB / LOW 예산 64MB
- GitHub Pages `/Defense/` 프로덕션 빌드

브라우저 검증 환경의 Chromium은 GPU와 SwiftShader 어느 쪽에서도 WebGL 컨텍스트를 만들 수 없었다. 이 상태에서는 실제 프로덕션 렌더러가 3단계 재시도 후 BOOT RECOVERY에 다음 원인을 정확히 표시하는 것을 확인했다.

```text
WebGL 렌더러 초기화 실패: THREE.WebGLRenderer: Error creating WebGL context.
```

동일한 게임 코드에서 렌더러와 필수 에셋 로딩만 테스트 대역으로 교체한 브라우저 스모크에서는 다음 흐름이 통과했다.

- 타이틀 부팅과 `worldReady`
- 원정 모드 버튼 3개
- 런 시작, 플레이어 그룹 생성
- 영구 성장 키 6개 마이그레이션
- 첫 웨이브 시작
- 징조와 도전 배정
- 적 5체 생성과 update 루프
- 적응형 FX 비율 반영
- 런타임 예외 0건

실기기에서는 실제 WebGL 출력과 터치 감각을 별도로 확인해야 한다.

### 실기기 확인 순서

1. `?fresh=1.9.0`으로 접속해 BOOT RECOVERY 없이 타이틀이 열리는지 확인한다.
2. 세 난이도를 눌러 요약과 시작 수치가 바뀌는지 확인한다.
3. 첫 웨이브에서 징조와 도전 HUD가 함께 표시되는지 확인한다.
4. 혼불을 100% 충전해 F키와 모바일 버튼으로 폭주가 발동하는지 확인한다.
5. 2웨이브 뒤 유물 3개가 나오고 선택 후 다음 웨이브가 열리는지 확인한다.
6. 3웨이브 축복, 4웨이브 계약, 5웨이브 유물이 순서대로 충돌 없이 열리는지 확인한다.
7. 결계·파열·고대 정예의 보호막, 사망 폭발, 강화 수치를 확인한다.
8. 320px급 화면과 왼손 모드에서 조이스틱과 5버튼 독이 겹치지 않는지 확인한다.
9. F3에서 엔진 1.2.0, 해상도 비율, FX 비율, 풀·청크 진단을 확인한다.
10. 결과 화면과 랭킹 데이터에 원정 모드와 새 통계가 저장되는지 확인한다.

### 다음 패치 예상 — v2.0.0 / Engine 1.3.0

1. 월식 보스 3종과 보스별 2페이즈 패턴
2. 징조별 조명·안개·하늘·전장 장식 테마
3. 실제 환경 GLB HIGH/MEDIUM/LOW 첫 슬롯
4. 런 시드와 오늘의 원정, 동일 시드 재도전
5. 유물 조합 세트와 저주받은 전설 유물
6. 엘리트 사망 폭발의 실제 위험 판정과 회피 보상
7. 실기기 성능 로그 JSON 내보내기
8. 접근성용 진동·화면 흔들림·섬광 강도 개별 설정

### 다음 작업자가 시작할 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. `?stats=1&fresh=1.9.0`으로 실기기 접속
5. 원정 모드 → 첫 도전 → 수호신 폭주 → 2웨이브 유물 → 3웨이브 축복 → 4웨이브 계약 순서 확인
6. 다음 버전은 게임 `2.0.0`, 엔진 `1.3.0`
7. 사용자에게 전체 ZIP과 패치 ZIP 두 개만 전달

---

## v2.0.0 / Engine 1.3.0 — 오늘의 원정과 3대 월식 보스 대규모 업데이트

### 패치 목표

v1.9.0의 원정·유물·도전 구조를 유지하면서 한 판을 다시 플레이할 이유와 보스전의 정체성을 크게 강화했다. 이번 버전의 핵심은 같은 시드를 공유하고 재현할 수 있는 원정, 4·7·10웨이브의 서로 다른 보스, 징조에 반응하는 전장, 강한 대가를 가진 저주 유물과 세트 조합이다.

현재 버전 기준:

- 게임 `2.0.0`
- 엔진 `1.3.0`
- 폴더 `DokkaebiLuckDefense3D_FULL_v2.0.0`

### 오늘의 원정과 런 시드

`src/daily-expedition.js`를 추가했다.

- `daily`: Asia/Seoul 날짜를 기준으로 `MOON-YYYYMMDD` 시드 생성
- `random`: 새 자유 원정 시드 생성
- 결과 및 일시정지 화면에서 같은 시드 재도전 지원
- FNV 계열 해시와 시드 난수 생성기 사용
- 징조, 도전, 유물 제시, 적 종류, 생성 문, 위치 편차, 정예 판정 등 핵심 난수 경로를 시드에 연결

동일 시드 재도전은 시각 파티클 같은 비핵심 난수까지 완전히 고정하는 기능은 아니다. 전투 규칙과 빌드 후보를 재현하는 것이 목표다.

### 오늘의 칙령 6종

매일의 시드로 하나의 칙령이 고정된다.

- 등불 질주: 빠른 전개와 보상 증가
- 수호 서약: 신목·방패·혼불 중심
- 영웅의 밤: 대장 공격과 스킬 중심
- 정예 행렬: 정예 확률·보상 증가
- 행운의 조수: 소환 운과 경제 강화
- 귀화 만조: 도깨비불과 수호신 폭주 강화

칙령은 적 체력·공격·속도·생성 간격, 보상·점수, 정예 확률, 혼불, 영웅 피해, 시작 자원 등에 선택적으로 곱해진다. 타이틀 미리보기, 전투 시드 칩, 결과 분석에 표시한다.

### 월식 보스 3종

`src/boss-director.js`에서 보스 웨이브와 프로필을 관리한다.

- 4웨이브 `저승 호랑이`: 돌진, 충격파, 2페이즈
- 7웨이브 `청월 이무기`: 독환, 혼령 속박, 월무, 2페이즈
- 10웨이브 `백귀왕`: 다중 위험 지대와 연속 패턴, 3페이즈

보스 웨이브에는 전용 추가 적 수와 보스 모델·패턴·의도 HUD가 적용된다. 결과 통계에는 보스 처치 수와 위험 패턴 피격 수가 남는다.

### 징조별 전장 테마

`src/battlefield-themes.js`를 추가했다. 기본 테마와 징조 8종에 대해 다음 값을 정의한다.

- 배경과 안개 색·밀도
- 반구광 하늘·지면색
- 월광, 달, 후광
- 요괴문 포털
- 전장 지면·외곽 룬·내부 원
- 도깨비불

징조가 시작되면 현재 색에서 목표 색으로 보간한다. 새 월드를 만들거나 타이틀로 돌아갈 때는 기본 테마를 즉시 적용한다.

### 유물 18종과 저주 전설 4종

기존 12종에 일반 2종과 저주 전설 4종을 더해 총 18종으로 확장했다.

저주 전설:

- 피의 거울
- 월식 가면
- 혼령 장부
- 깨진 종

보스 웨이브 직후 유물 제시에는 아직 획득하지 않은 저주 유물 하나가 보장된다. 강한 화력·경제·혼불 효과와 함께 생존, 쿨다운, 자원 등 반대급부를 가진다.

### 유물 세트 4종

두 개의 관련 유물을 모으면 세트가 활성화된다.

- 행운 세트
- 전쟁 세트
- 수호 세트
- 혼령 세트

세트는 중복 계산하지 않으며 활성 순간 전투 알림과 통계를 기록한다. 유물 스트립에서 저주 유물과 활성 세트를 시각적으로 구분한다.

### 파열 정예 실제 폭발 판정

v1.9.0에서는 파열 정예 사망 시 경고 장판과 기절 연출만 있었다. v2.0.0부터는 경고 후 실제 판정을 실행한다.

범위 안:

- 플레이어 기절
- 스킬 재사용 대기시간 증가
- 혼불 손실
- 엽전 손실
- 피격 통계 증가

범위 밖:

- 회피 점수
- 혼불 보상
- 위험 회피 통계 증가

신목 가까이에서 폭발하면 신목에도 일부 피해가 들어간다. 사망 전 적 위치를 보존하므로 풀 반환 좌표 초기화와 충돌하지 않는다.

### 접근성 설정

카메라·조작 설정 모달에 다음 항목을 추가했다.

- 화면 흔들림 0~100%
- 피격 섬광 0~100%
- 진동 피드백 ON/OFF
- 모션 감소 ON/OFF

설정은 로컬에 저장된다. 모션 감소는 파티클 수와 시네마틱 움직임을 줄이며, 진동 호출은 지원 기기와 설정이 모두 허용할 때만 실행한다.

### 성능 로그 JSON

일시정지 화면에 성능 로그 내보내기를 추가했다. 다음 정보를 JSON으로 저장한다.

- 게임·엔진 버전
- 시드 방식, 시드, 칙령, 난이도
- 웨이브, 점수, 런 통계
- 엔진 품질 배율과 이펙트 예산
- 렌더러 정보
- 에셋, 청크, 애니메이션, 오브젝트 풀 진단
- 사용자 조작·접근성 설정

실기기 성능 제보를 재현할 때 사용한다.

### Engine 1.3.0

- 엔진 버전을 `1.3.0`으로 갱신
- `globalThis.__DOKKAEBI_RENDERER_FACTORY__`가 명시적으로 설정된 검증 환경에서만 테스트 렌더러 주입 가능
- 일반 배포에서는 기존 WebGL 3단계 생성 경로를 그대로 사용
- 주입 렌더러 경로는 GPU 없는 자동 검증에서 실제 번들의 DOM·게임 로직을 실행하기 위한 테스트 가능성 개선이며 게임 플레이 경로에는 영향을 주지 않음

### UI와 결과 분석

- 타이틀에 오늘의 원정 / 자유 원정 선택 추가
- 오늘의 칙령 미리보기 추가
- 전투 HUD에 시드·칙령 고정 칩 추가
- 저주 유물과 활성 세트 스타일 추가
- 결과 화면에 시드, 칙령, 보스 처치, 파열 피격·회피, 보스 위험 피격, 활성 세트 표시
- 결과 화면에 같은 시드 재도전과 새 시드 원정 버튼 분리
- 일시정지 화면에도 같은 시드 재시작 버튼 제공
- 320px급 화면과 가로 저높이 화면 규칙을 v2 UI에 맞게 재검증

### 새 파일

- `src/daily-expedition.js`
- `src/boss-director.js`
- `src/battlefield-themes.js`
- `scripts/verify-daily-expedition.mjs`
- `scripts/verify-boss-themes.mjs`

### 주요 변경 파일

- `package.json`
- `package-lock.json`
- `index.html`
- `README.md`
- `PROJECT_HANDOFF.md`
- `src/main.js`
- `src/style.css`
- `src/game-data.js`
- `src/expedition-director.js`
- `src/engine/engine-config.js`
- `src/engine/mobile-engine.js`
- `scripts/verify-project.mjs`
- `scripts/verify-expedition-system.mjs`
- `scripts/verify-mobile-layout.mjs`

### 검증 결과

실행 명령:

```bash
npm ci
npm run verify
VITE_BASE_PATH=/Defense/ npm run build
```

통과 항목:

- npm 의존성 취약점 0건
- DOM ID 연결 163개
- 게임 `2.0.0`, 엔진 `1.3.0`
- 원정 난이도 3종
- 시드 방식 2종과 오늘의 칙령 6종
- 같은 시드에서 핵심 선택 시퀀스 재현
- 4·7·10웨이브 보스 프로필과 2·2·3페이즈 구성
- 징조별 전장 테마 8종
- 유물 18종, 저주 전설 4종, 세트 4종
- 웨이브 도전 6종, 징조 8종, 정예 속성 7종
- 접근성 설정과 저장 경로
- 성능 로그 JSON 내보내기
- 실제 파열 폭발 피격·회피 경로
- 320·350·360·390·410px 세로 모바일 배치
- 568px 가로 모바일 배치
- 카메라, 입력 방향, 애니메이션 상태
- 도깨비 300 / 일반 적 500 triangle 예산
- 래스터 텍스처 메모리 14.02MB / LOW 예산 64MB
- GitHub Pages `/Defense/` 프로덕션 빌드

GPU가 없는 Chromium 환경에서는 실제 WebGL 컨텍스트를 만들 수 없었다. 일반 렌더러 경로가 3단계 시도 후 BOOT RECOVERY로 이동하는 동작을 확인했고, 동일한 프로덕션 번들에 테스트 렌더러만 주입한 브라우저 스모크에서는 다음 흐름을 통과했다.

- 타이틀 부팅과 월드 생성
- 난이도 3종, 시드 방식 2종, 접근성 UI
- 오늘의 원정 시작과 시드·칙령 표시
- 4웨이브 저승 호랑이 생성
- 7웨이브 청월 이무기 생성
- 10웨이브 백귀왕 생성
- 징조 전장 테마 연결
- 보스 뒤 저주 유물 포함 3개 제시와 선택
- 수호신 폭주 발동
- 파열 폭발 피격과 회피 보상
- 동일 시드 재도전
- 런타임 예외 0건

실제 WebGL 출력, 보스 패턴의 체감 난이도, 터치 조작 감각은 실기기에서 최종 확인해야 한다.

### 실기기 확인 순서

1. `?fresh=2.0.0`으로 접속해 BOOT RECOVERY 없이 타이틀이 열리는지 확인한다.
2. 오늘의 원정과 자유 원정 버튼, 칙령 미리보기가 표시되는지 확인한다.
3. 같은 시드 재도전에서 첫 징조·도전·유물 후보가 재현되는지 확인한다.
4. 4웨이브 저승 호랑이의 2페이즈와 돌진·충격파를 확인한다.
5. 7웨이브 청월 이무기의 독환·속박·월무를 확인한다.
6. 10웨이브 백귀왕의 3페이즈와 연속 위험 지대를 확인한다.
7. 징조가 바뀔 때 안개·달빛·포털·지면색이 부드럽게 변하는지 확인한다.
8. 보스 뒤 저주 유물 후보가 포함되고 두 유물 세트가 활성화되는지 확인한다.
9. 파열 정예 폭발 안팎의 피격·회피 보상과 신목 피해를 확인한다.
10. 화면 흔들림, 섬광, 진동, 모션 감소 설정과 재접속 저장을 확인한다.
11. 일시정지에서 성능 JSON을 내려받아 버전·시드·엔진 진단이 들어 있는지 확인한다.
12. 320px급 화면과 왼손 모드에서 새 시드 칩과 5버튼 독이 겹치지 않는지 확인한다.

### 다음 패치 예상 — v2.1.0 / Engine 1.3.1

1. 보스별 전용 보상과 도감
2. 사용자 입력 가능한 공유 시드 코드
3. 저주 유물 해제·정화 선택
4. 징조별 전장 장식과 첫 환경 GLB LOD 슬롯
5. 일일 원정 로컬 최고 기록과 연속 출석
6. 보스 패턴 접근성 텔레그래프 강화
7. 저장 데이터 버전 마이그레이션 진단 화면

### 다음 작업자가 시작할 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. `?stats=1&fresh=2.0.0`으로 실기기 접속
5. 오늘의 원정 → 4웨이브 보스 → 저주 유물 → 7웨이브 보스 → 세트 활성 → 10웨이브 보스 순서 확인
6. 다음 버전은 게임 `2.1.0`, 엔진 `1.3.1`
7. 사용자에게 전체 ZIP과 패치 ZIP 두 개만 전달


---

## v2.1.0 / Engine 1.4.0 — 달빛 대도감과 에셋 제작 기반

### 패치 목표

v2.0.0까지는 플레이 루프와 보스·시드·유물 구조를 확장했다. v2.1.0은 콘텐츠 수를 무작정 늘리기 전에 실제 고품질 GLB와 환경 타일을 안전하게 교체할 수 있도록 아트 방향, 모델 슬롯, LOD, 도감, 검증 규칙을 하나로 고정한다.

현재 버전 기준:

- 게임 `2.1.0`
- 엔진 `1.4.0`
- 폴더 `DokkaebiLuckDefense3D_FULL_v2.1.0`

### 1. 일시정지 카메라 설정 팝업 수정

원인은 `.modal` 공통 레이어가 130인데 과거 호환 CSS에서 `#controls-modal`만 118로 내려가 있던 것이다. 일시정지 모달 위에서 카메라 설정을 열면 설정 화면이 실제로는 뒤에 존재해 클릭할 수 없었다.

수정 내용:

- 모달을 열린 순서대로 관리하는 `modalStack` 추가
- 각 모달에 `--modal-layer`를 동적으로 배정
- 일시정지에서 설정을 열 때 부모 모달을 `modal-obscured`로 처리
- 설정 종료 후 부모 일시정지 모달의 접근성 상태 복원
- 모달을 연 버튼으로 포커스 복귀
- ESC는 가장 위의 하위 모달부터 닫고, 일시정지 모달에서만 게임 재개
- `#controls-modal`의 과거 고정 z-index를 스택 기반 값으로 교체

### 2. 타이틀 화면 정보 정리

- 패치명, 업데이트 홍보 문장, 적용 기능 나열 제거
- 타이틀 하단은 `v2.1.0` 버전만 표시
- `도깨비 도감` 버튼을 `달빛 도감`으로 변경
- BOOT RECOVERY 인라인 스크립트에 중복 선언되어 있던 `retryButton` 한 줄도 제거

### 3. 달빛 장터 대도감

도감은 다음 다섯 탭으로 확장했다.

- 수호대 6종
- 일반 요괴 4종
- 월식 보스 3종
- 전장 오브젝트 8종
- 기술·발사체 효과 8종

총 29개 항목이며, 각 카드에는 다음 정보를 표시한다.

- 이름과 전투 분류
- 게임 내 설명
- 모델링에서 우선할 실루엣
- 한국 민담·조선 야시장 모티프
- 대표 발광·발사체·장판 연출
- 작은 화면에서 반드시 읽혀야 하는 전투 판독 정보

데이터는 `src/codex-data.js`에 분리했다.

### 4. 아트 방향

프로젝트의 시각 키워드를 **달빛 야시장 민담 판타지**로 고정한다.

- 수호 도깨비: 둥글고 낮은 무게중심
- 적 요괴: 날카롭고 비대칭인 실루엣
- 따뜻한 한지 등불과 차가운 월광의 이중 조명
- 갓, 무속 방울, 부적, 금줄, 장승, 귀면와, 청자, 달항아리 사용
- 발광은 얼굴·무기·공격 시작점 중심으로 전체 면적 12% 안쪽
- 작은 모바일 화면에서 잔무늬보다 큰 형태와 색 덩어리를 우선

상세 기준은 `docs/ASSET_BIBLE.md`에 기록했다.

### 5. 모델·텍스처 목표

#### LOD0 근거리

- 수호대 4,000~7,000 triangles
- 일반 요괴 3,200~5,200 triangles
- 보스 12,000~18,000 triangles
- 수호대·일반 요괴 1024² KTX2
- 보스 2048² KTX2

#### LOD1 중거리

- 수호대 1,600~2,800 triangles
- 일반 요괴 1,200~2,000 triangles
- 보스 5,000~8,000 triangles

#### LOD2 원거리

- 11방향 임포스터 아틀라스
- 360 / 11 = 약 32.73도 간격
- 2048² KTX2 또는 WebP
- 정면과 후면 경계에서 프레임이 떨리지 않도록 히스테리시스 적용

중요: 11방향 이미지는 주 캐릭터 표현이 아니라 원거리 모바일 LOD다. 근거리 캐릭터와 보스는 자유 회전과 애니메이션을 위해 실제 리깅 GLB를 사용한다.

### 6. 엔진 준비

새 파일:

- `src/asset-specs.js`
- `src/codex-data.js`
- `src/engine/directional-impostor.js`
- `docs/ASSET_BIBLE.md`
- `docs/ASSET_MANIFEST.json`
- `scripts/verify-asset-readiness.mjs`

`MODEL_ASSET_SLOTS`를 다음 단위로 세분화했다.

- 수호대 6
- 일반 요괴 4
- 보스 3
- 환경 8
- 효과 8

현재 슬롯의 `variants`는 비어 있으므로 절차형 폴백이 계속 사용된다. 실제 GLB/KTX2가 준비되면 각 슬롯의 low, medium, high URL을 채우고 모델 생성부를 슬롯 인스턴스로 교체한다.

### 7. 첫 실제 에셋 제작 순서

1. 불씨 깨비 LOD0/LOD1/11방향 LOD2
2. 장난 요괴 LOD0/LOD1/11방향 LOD2
3. 천년 신목과 월문 석판 타일
4. 도깨비불 탄환과 공통 충돌 효과
5. 저승 호랑이 보스
6. 나머지 수호대 5종
7. 나머지 일반 요괴 3종
8. 청월 이무기와 백귀 야행왕
9. 야시장 가판대·등주·장승·달항아리·소품군
10. 속성별 발사체와 보스 전용 위험 장판

첫 4개를 먼저 제작해야 캐릭터, 적, 핵심 배경, 발사체가 한 화면에서 동시에 바뀌어 아트 방향을 빠르게 판단할 수 있다.

### 8. 검증 추가

`verify-asset-readiness.mjs`가 다음을 확인한다.

- 11방향 프레임 규격과 360도 프레임 선택
- 근·중·원거리 3단 LOD
- 캐릭터별 필수 애니메이션
- 수호대·요괴·보스·환경·효과 슬롯 수
- 8m 환경 타일과 아틀라스 규격
- 에셋 바이블과 납품 매니페스트 존재

### 다음 초대규모 패치 방향 — v2.3.0 / Engine 1.5.0

1. 불씨 깨비와 장난 요괴의 첫 실제 리깅 GLB 교체
2. LOD0 → LOD1 → 11방향 임포스터 실제 전환
3. 천년 신목과 월문 석판의 첫 환경 GLB·타일 세트
4. 도깨비불 탄환과 공통 충돌 FX 아틀라스
5. 도감에 실제 3D 회전 미리보기와 발견 기록
6. 일반 요괴 4종에 원거리·분열·보호·지원 역할을 더해 전투 실루엣 확장
7. 보스별 전용 전리품과 도감 약점 정보
8. 에셋 로딩 실패 시 슬롯 단위 절차형 폴백 진단

### 다음 작업자가 시작할 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. 일시정지 → 카메라 설정 → 설정 종료 → 계속 수호하기 순서 확인
5. 타이틀에 버전 외 패치 문장이 없는지 확인
6. 달빛 도감의 5개 탭과 29개 항목 확인
7. 첫 에셋은 `ember`, `imp`, `sacred-tree`, `ground-tile`, `effect-ember-orb` 슬롯부터 연결
8. 다음 버전은 게임 `2.3.0`, 엔진 `1.5.0`
9. 사용자에게 전체 ZIP과 패치 ZIP 두 개만 전달

---

## v2.3.0 / Engine 1.5.0 — Moon Forge 비주얼 퀄리티 점프

### 목표

v2.1.0의 에셋 바이블과 슬롯을 실제 게임 화면에서 검증 가능한 제작 세트로 연결했다. 콘텐츠 숫자보다 수호대·요괴·보스·신목·바닥·발사체의 실루엣과 판독성을 높이고, 이후 작가 제작 GLB를 모델 슬롯에 교체할 수 있는 런타임 경로를 완성했다.

### 핵심 변경

- `src/premium-assets.js`: 수호대 6, 요괴 4, 보스 3, 천년 신목 절차형 모델
- `src/codex-viewer.js`: 도감 3D 회전·확대·idle/move/attack/skill 감상
- `moon-market-ground-v1.webp`: 실제 전장 바닥 반복 텍스처
- `moon-fx-atlas-v1.webp`: 발사체와 충돌 효과 4×4 아틀라스
- `ember-idle-11.webp`: 불씨 깨비 11방향 원거리 아틀라스
- 불씨 깨비는 일반 약 22m, 저사양 약 16m 이후 LOD2 임포스터 사용
- 상태 애니메이션에서 weapon, shoulders, signature, rankBeads, halo 파트 추가
- 엔진 예산: 수호대 1,700 / 요괴 800 / 보스 3,000 triangles

### 검증 결과

- 클린 `npm ci` 성공, 취약점 0건
- `npm run verify` 전체 통과
- 실제 생성 모델 측정: 수호대 최대 1,398, 요괴 최대 538, 보스 최대 1,972, 신목 1,888 triangles
- 텍스처 예산 40.69MB / 64MB
- 320~410px 세로 및 568px 가로 모바일 배치 통과
- `/Defense/` 프로덕션 빌드 성공
- 빌드 산출물에 월문 바닥, FX 아틀라스, 불씨 11방향 아틀라스 포함

### 알려진 한계

- 현재 캐릭터는 최종 리깅 GLB가 아니라 게임에서 작동하는 절차형 제작 프로토타입이다.
- 실전 11방향 아틀라스는 불씨 깨비 idle 한 종이다.
- 최종 그래픽 출력과 터치 감각은 실제 WebGL 모바일 기기에서 확인해야 한다.

### 다음 패치 방향 — v2.3.0 / Engine 1.6.0

1. 불씨 깨비 작가 제작 리깅 GLB LOD0/LOD1
2. 장난 요괴 작가 제작 리깅 GLB LOD0/LOD1
3. 저승 호랑이 보스 GLB와 phase 애니메이션
4. 불씨·장난 요괴 move/attack 11방향 아틀라스
5. 장터 가판대·등주·장승·달항아리·광주리 환경 세트
6. 속성별 발사체·명중·소환 KTX2 아틀라스
7. 도감 발견도, 약점, 보스 전리품과 애니메이션 속도 조작

### 다음 작업 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. 실기기에서 3D 도감과 불씨 깨비 거리 LOD 확인
5. 다음 버전은 게임 `2.3.0`, 엔진 `1.6.0`
6. 전체 ZIP과 변경분 패치 ZIP 두 개만 전달


---

## v2.3.0 / Engine 1.6.0 — Moonlit Mascot UI·전장 가독성 패스

### 사용자 요구

- 모바일 첫 화면이 뷰포트를 벗어나는 문제 해결
- UI가 겹칠 가능성이 있는 구간 재배치
- 두 손가락 줌 방향 점검 및 자연스러운 방향으로 수정
- 첫 화면 카메라 설정을 우측 상단 고정 톱니바퀴로 통합
- 중앙 보호 신목 크기 축소
- 마스코트 도깨비와 에셋 제작 계속 진행
- 불씨 깨비와 일반 요괴의 이동·공격 11방향 자산 확장

### 모바일 UI 변경

- `applyViewportUiProfile()`이 `visualViewport`의 실제 폭·높이를 우선 사용한다.
- 화면 조건에 따라 `ui-compact`, `ui-ultra-compact`, `ui-landscape-compact` 클래스를 적용한다.
- 320×568 초컴팩트 화면에서는 kicker, 설명, 일일 칙령 카드를 숨기고 런 미리보기는 핵심 3개만 남긴다.
- 난이도·시드·시작·도감·성장 버튼의 높이와 간격을 축소하지만 터치 버튼은 36~44px 이상을 유지한다.
- 카메라 설정 버튼은 `⚙️` 아이콘의 44×44px 고정 버튼이며 safe-area를 반영한다.
- 보스 활성 중에는 `boss-active` 클래스로 시너지·수호대·연속 처치 HUD를 보스 체력바 아래 레일로 이동한다.
- 웨이브·운·시드·징조·도전·유물·첫 임무 패널의 모바일 좌표와 폭을 별도로 지정했다.

### 카메라 조작

- 메인 전투 핀치 계산을 `기준 거리 - 손가락 간격 증가량`으로 수정했다.
- 도감 3D 뷰어도 같은 방향을 사용한다.
- 결과: 손가락을 벌리면 줌인, 오므리면 줌아웃한다.
- 설정 설명 문구도 실제 동작과 일치하게 변경했다.

### 중앙 신목 축소

- `createPremiumSacredTree()` 결과에 `scale 0.78` 적용
- 이동 장애물 반경: 2.35 → 1.82
- 카메라 장애물 반경: 2.8 → 2.15
- 카메라 가림 높이: 8.6 → 6.7
- 피해 숫자 높이: `damageAnchorY 4.55`
- 피격 파티클 높이: `impactY 3.7`
- 중앙이 비어 보이지 않도록 외곽에 장승 4기, 달항아리 6개, 소형 마스코트 제단 추가

### 마스코트·앱 자산

새 제작 스크립트:

- `scripts/generate_moon_forge_assets.py`

생성 및 적용 자산:

- `src/assets/moon-mascot-v1.webp` — 768×768 투명 마스코트
- `public/icon-192.png`
- `public/icon-512.png`
- `public/icon-maskable-512.png`
- `public/cover.webp`

마스코트 디자인 기준:

- 3등신 이하의 둥근 수호 도깨비
- 작은 황금 뿔과 보라색 도포
- 엽전·부적·청록 혼불을 통해 게임 루프 표현
- 작은 앱 아이콘에서도 눈, 뿔, 엽전이 구분되는 큰 형태 우선

### 상태별 11방향 임포스터

새 런타임 자산:

- `guardian/ember-idle-11.webp`
- `guardian/ember-move-11.webp`
- `guardian/ember-attack-11.webp`
- `monster/imp-idle-11.webp`
- `monster/imp-move-11.webp`
- `monster/imp-attack-11.webp`

공통 규격:

- 1024×768 WebP
- 4×3 셀
- 실제 프레임 11개
- idle/move/attack 상태 전환
- 일반 품질 약 21~22m, 저사양 약 15~16m에서 LOD2 진입
- 2.5m 히스테리시스로 경계 떨림 방지
- 부모 그룹의 월드 회전을 역보정한 뒤 카메라 쿼터니언을 적용해 빌보드 누적 회전 방지
- 도감에서도 불씨 깨비와 장난 요괴의 상태별 모델/임포스터 비교 지원

### 검증 추가

새 파일:

- `scripts/verify-ui-v230.mjs`

검증 내용:

- 고정 톱니바퀴와 safe-area
- visualViewport 기반 프로필
- 320×568, 360×640, 390×844, 430×740 첫 화면 높이 예산
- 핀치 방향 일치
- 보스 HUD 충돌 회피
- 신목 스케일·충돌·피격 앵커
- 장승·달항아리·마스코트 제단
- 마스코트 768×768
- 상태별 아틀라스 6개의 1024×768 크기
- 수호대·요괴 LOD 부모 회전 보정

### 현재 에셋 단계와 한계

- 마스코트와 아틀라스는 게임에 포함된 실제 래스터 자산이다.
- 캐릭터 본체는 아직 최종 작가 리깅 GLB가 아니라 절차형 런타임 프로토타입이다.
- 11방향 자산은 프레임 구조와 상태 전환을 검증하기 위한 제작 패스이며 최종 단계에서 KTX2와 작가 렌더로 교체한다.
- 최종 색감, 모바일 브라우저 주소창 변화, 핀치 감도, LOD 전환 체감은 실제 iOS/Android WebGL 기기 확인이 필요하다.

### 다음 패치 방향 — v2.5.0 / Engine 1.7.0

1. 불씨 깨비와 장난 요괴의 첫 리깅 GLB LOD0/LOD1 교체
2. 마스코트 이름과 짧은 표정·로딩 애니메이션 확정
3. 저승 호랑이 보스의 phase 리깅 세트
4. 야시장 가판대·등주·포목·광주리의 모듈형 환경 세트
5. 도감 발견도·약점·전리품 실제 저장과 보상 연결
6. 속성별 발사체 및 명중 KTX2 아틀라스
7. 실기기 스크린샷 기준 UI 자동 회귀 검증

### 다음 작업 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. 320×568 세로, 390×844 세로, 844×390 가로 실기기 확인
5. 타이틀 톱니바퀴 → 설정 모달 → 닫기 포커스 복귀 확인
6. 두 손가락 벌리기 줌인/오므리기 줌아웃 확인
7. 보스전에서 중앙 체력바와 좌우 HUD 비중첩 확인
8. 불씨·장난 요괴 idle/move/attack LOD 확인
9. 다음 버전은 게임 `2.5.0`, 엔진 `1.7.0`
10. 전체 ZIP과 변경분 패치 ZIP 두 개만 전달

---

## v2.5.0 / Engine 1.7.0 — 달빛 도감 사냥·보스 페이즈 패스

### 사용자 흐름 목표

v2.3.0에서 확보한 모바일 한 화면 UI와 마스코트·11방향 LOD를 유지하면서, 도감을 단순 감상 메뉴가 아니라 전투 중 발견하고 연구하며 영구 보상을 쌓는 장기 루프로 전환했다. 최종 작가 GLB를 기다리는 동안에도 전투·수집·비주얼의 연결을 먼저 검증할 수 있도록 구성했다.

### 도감 영구 진행

새 모듈:

- `src/codex-progression.js`

저장 키:

- `dokkaebi-codex-progress-v1`

기록 항목:

- 전체 29개 항목의 발견 여부
- 조우·격파·강림 횟수
- 4단계 숙련
- 약점 해독 여부
- 전리품 10종 보유 수량
- 최초 발견 시각과 마지막 저장 시각

전장·효과 16종은 기본 발견으로 시작하고, 수호대·일반 요괴·보스 13종은 실제 강림 또는 조우로 열린다. 도감 카드는 미발견 상태에서 실루엣과 해금 조건만 표시하며 3D 뷰어 진입을 막는다.

### 약점 연구와 실제 전투 보정

총 7종 연구를 연결했다.

- 장난 요괴 → 달서리 1.22배
- 두억 질주꾼 → 바람 1.22배
- 돌갑옷 귀수 → 천뢰 1.28배
- 저주 무당 → 도깨비불 1.24배
- 저승 호랑이 → 달서리 1.18배
- 청월 이무기 → 천뢰 1.18배
- 백귀 야행왕 → 방울 1.16배

일반 요괴는 3회, 보스는 1회 격파하면 해독된다. `damageEnemy()`에서 공격 수호대 종류와 해독 상태를 확인해 실제 피해 배율을 적용한다. 약점 적중은 과도한 텍스트를 막는 개별 쿨다운을 사용하며 결과 통계에 누적한다.

### 전리품 10종과 혼불 조각

일반 드롭:

- 깨진 장난탈
- 밤질주 짚신
- 귀수 돌매듭
- 젖은 저주부

보스 첫 격파 보장:

- 저승 산군의 엄니
- 청월 비늘
- 월식 왕가면

희귀 드롭:

- 혈월 갈기
- 혼령 여의주 조각
- 백귀 행진 두루마리

드롭 수량은 영구 저장되며, 각 전리품에 정의된 혼불 조각을 즉시 지급한다. 보스 첫 격파 증표는 `firstOnly` 규칙으로 한 번만 100% 보장하고 이후 중복 지급하지 않는다.

### 도감 UI 확장

추가 DOM:

- `codex-progress-readout`
- `codex-weakness-readout`
- `codex-loot-readout`
- `codex-research-tip`

도감 목록에는 발견률, 약점 해독 수, 전리품 보유 종류를 표시한다. 각 카드에는 숙련도 4칸, 조우·격파·강림 기록, 약점 상태, 드롭 보유 수가 나타난다. 보스 3D 미리보기는 해금된 숙련 범위와 실제 보스 페이즈 수 안에서 페이즈 모델을 보여준다.

### 보스 페이즈 시각 구조

`src/premium-assets.js`에 `applyPremiumBossPhase()`를 추가했다.

- 저승 호랑이 2페이즈: 혈월 고리, 등불 갈기, 발광 눈띠
- 청월 이무기 2페이즈: 회전 월륜 3개
- 백귀 야행왕 2·3페이즈: 궤도 가면과 왕관 고리

적 모델 풀 반환 시 보스 페이즈 비주얼을 1페이즈로 초기화하고 동적 geometry/material을 해제한다. 따라서 이전 웨이브에서 사용한 보스가 다음 런에 재사용돼도 장식과 발광 상태가 남지 않는다.

### 모듈형 달빛 장터

`createMoonMarketModuleSet()`으로 전장 외곽 환경 조합을 추가했다.

- 월문 지붕 가판대 4개
- 매달린 등롱
- 한글풍 세로 간판
- 포목·광주리·항아리 상품 묶음 12세트
- 전투 중앙과 이동 통로를 가리지 않는 외곽 배치

현재는 절차형 low-poly 환경 모듈이며, 최종 가판대 GLB가 들어오면 동일 루트 아래에서 교체한다.

### 마스코트 로딩 표정

새 자산:

- `src/assets/moon-mascot-expressions-v1.webp`
- 1536×1536, 2×2, idle/blink/surprise/cheer 4상태

`loading-mascot` CSS 애니메이션으로 부팅 중 표정을 순환한다. 제작 스크립트 `scripts/generate_moon_forge_assets.py`가 기본 마스코트, 앱 아이콘, 커버, 6개 임포스터와 함께 표정 아틀라스도 재생성한다.

### 런 통계 추가

- `codexDiscoveries`
- `weaknessUnlocks`
- `weaknessHits`
- `codexDrops`

런 결과 분석에는 신규 발견 수, 약점 해독 수, 약점 공격 적중, 전리품 획득 수가 포함된다.

### 검증과 예산

- `npm ci` 성공
- 의존성 취약점 0건
- `npm run verify` 전체 통과
- DOM 연결 178개
- 도감 29종, 기본 발견 16종
- 약점 연구 7종, 전리품 10종
- 격파 시 조우 횟수 중복 증가 방지 검증
- 보스 첫 격파 보장 전리품 검증
- 수호대 최대 1,398 / 1,700 triangles
- 일반 요괴 최대 538 / 800 triangles
- 보스 최대 1,972 / 3,000 triangles
- 신목 1,888 / 9,000 triangles
- 텍스처 메모리 63.69MB / 64MB
- 320~410px 세로, 568px 가로 모바일 배치 통과

### 알려진 한계

- 캐릭터 본체는 아직 최종 작가 리깅 GLB가 아니라 절차형 제작 프로토타입이다.
- 텍스처 메모리 예산이 64MB에 매우 가까우므로 다음 래스터 추가 전 KTX2 또는 해상도 축소가 필요하다.
- 전리품은 현재 수집과 혼불 조각 지급까지 연결됐으며 장착·세트 제작 기능은 아직 없다.
- 자동 검증 환경에서는 실제 모바일 GPU 출력과 터치 체감을 캡처하지 못한다.

### 다음 패치 방향 — v2.5.0 / Engine 1.8.0

1. 전리품 조합·정화·장착 슬롯과 도감 세트 보상
2. 저승 호랑이 LOD0/LOD1 작가 납품 규격 및 페이즈 애니메이션 클립 슬롯
3. 불씨 깨비·장난 요괴 리깅 GLB 로더 우선 경로
4. 기존 WebP 임포스터 KTX2 전환으로 텍스처 메모리 확보
5. 도감 필터·정렬·새 발견 알림과 보스 재생 전시
6. 야시장 가판대·등주·포목·광주리 GLB 모듈 교체
7. 실기기 스크린샷 기준 UI 회귀 검증

### 다음 작업 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. 신규 저장 키의 첫 실행·기존 저장 마이그레이션 확인
5. 일반 요괴 3회 격파 후 약점 보너스 확인
6. 4·7·10웨이브 첫 보스 전리품과 중복 방지 확인
7. 풀링된 보스 재등장 시 1페이즈 비주얼 초기화 확인
8. 도감 미발견 카드와 3D 상세 판넬 확인
9. 다음 버전은 게임 `2.5.0`, 엔진 `1.8.0`
10. 전체 ZIP과 변경분 패치 ZIP 두 개만 전달

---

## v3.0.0 / Engine 2.0.0 — NextGen 아트 리워크

### 패치 목표

v2.5.0까지의 자산은 시스템과 제작 파이프라인을 검증하는 Moon Forge 프로토타입이었다. 사용자 피드백에 따라 v3.0.0에서는 구체와 원기둥을 조합한 인상을 줄이는 수준이 아니라, 현대 모바일 3D 액션 게임에서 기대하는 큰 실루엣, 다층 의상, 분리된 재질 반응, 월광 림, 환경 건축과 발사체 계층을 하나의 규격으로 다시 설계했다.

다만 이번 GLB는 작가 제작 스켈레탈 리깅 캐릭터라고 과장하지 않는다. 세 대표 모델은 실제 GLB 파일이며 파츠 노드가 분리되어 있지만, 현재 애니메이션은 기존 상태 머신이 파츠 transform을 절차적으로 구동한다. 최종 단계에서는 같은 에셋 슬롯에 본·스킨·애니메이션 클립이 포함된 납품 GLB를 넣는다.

### 실제 GLB 런타임 교체

새 파일:

- `public/assets/models/guardian-ember-nextgen.glb`
- `public/assets/models/monster-imp-nextgen.glb`
- `public/assets/models/boss-tiger-nextgen.glb`
- 생성기: `scripts/generate_nextgen_models.py`

검증 수치:

- 불씨 깨비: 파츠 노드 9개, 4,816 / 5,600 triangles, bounds 2.41×2.85×2.15m
- 장난 요괴: 파츠 노드 8개, 2,252 / 3,200 triangles, bounds 1.94×2.20×1.76m
- 저승 호랑이: 파츠 노드 7개, 6,404 / 9,000 triangles, bounds 2.57×2.74×4.10m

`src/engine/asset-catalog.js`의 코어 카탈로그에 등록했고, `src/main.js` 전투 생성과 `src/codex-viewer.js` 미리보기가 같은 GLB 인스턴스를 사용한다. 로딩 실패 시 `src/premium-assets.js`의 절차형 모델로 안전하게 폴백한다.

### 전체 캐릭터 형태와 애니메이션 리워크

`src/premium-assets.js`:

- 수호대 6종에 팔·다리·소매·깃·허리띠·어깨·머리 장식·속성 무기 레이어 추가
- 일반 요괴 4종에 역할별 팔다리 비율, 의상 조각, 가면·얼굴판, 주술 장식 추가
- 보스 페이즈 장식은 GLB와 절차형 모델 모두 지원
- MeshStandardMaterial 기반 roughness·metalness 재질 규칙 통일
- `onBeforeCompile`을 이용한 스타일라이즈드 월광 림 셰이더 적용

`src/engine/animation-state-system.js`:

- base transform을 파츠별로 기록
- idle, move, attack, hit, death에서 머리·팔·다리·천·어깨·무기·대표 장식을 분리 구동
- 기존 전투 판정 시점과 공격 동작을 유지
- GLB 파츠 이름과 절차형 파츠 이름을 같은 semantic role로 정규화

### 렌더링과 도감 품질

- 엔진 버전 `2.0.0`
- 데스크톱 그림자 활성화, `PCFSoftShadowMap`
- ACES Filmic 톤매핑과 노출 보정
- 저사양·모바일 그림자 비활성 정책 유지
- GLB는 일반 품질에서 castShadow 적용
- 도감 뷰어에도 ACES, 1024 그림자 맵, warm key + cool rim 적용
- `src/engine/engine-config.js` 예산을 수호대 5,600, 일반 요괴 3,200, 보스 9,000 triangles로 상향

### NextGen 야시장 환경

`createNextGenEnvironmentPass()`를 통해 외곽 장터를 다시 구성했다.

- 한국형 겹지붕 누각 4개
- 처마 곡선과 황동·옥 장식
- 다층 등롱과 현수막
- 바닥 월륜과 야시장 중심선
- 요괴문 지붕, 내부 룬, 귀면 문장
- 중앙 전투 시야와 이동 경로를 가리지 않는 외곽 배치

새로 생성·교체한 래스터:

- `public/assets/textures/moon-market-ground-v1.webp`
- `public/assets/effects/moon-fx-atlas-v1.webp`
- `src/assets/moon-market-keyart.webp`
- `public/cover.webp`
- 생성기: `scripts/generate_nextgen_visuals.py`

### 발사체와 VFX

- 발사체에 핵, 궤도 링, 잔상 submesh 추가
- additive blending과 depthWrite 비활성으로 밝은 에너지 층 분리
- 비행 중 링 회전, 핵 맥동, 꼬리 길이 변화
- 속성별 형태와 명도 차 유지
- 신규 FX 아틀라스는 기존 1024² 예산 안에서 교체

### 검증 자동화

새 스크립트:

- `scripts/verify-nextgen-assets.mjs`

검증 항목:

- GLBLoader로 실제 GLB 파싱
- 파츠 노드 수와 필수 semantic node
- bounding box
- triangle 예산
- asset catalog 등록
- 전투·도감 런타임 연결
- 파츠 애니메이션과 월광 림 셰이더 존재
- 환경·발사체 NextGen 패스 존재

최종 자동 검증:

- `npm ci` 성공
- `npm run verify` 전체 통과
- DOM 연결 178개
- GLB 3종 검증 통과
- 절차형 수호대 최대 약 3,038 / 5,600 triangles
- 절차형 일반 요괴 최대 약 1,158 / 3,200 triangles
- 절차형 보스 포함 적 모델 최대 약 2,592 / 9,000 triangles
- 신목 1,888 / 9,000 triangles
- 텍스처 메모리 63.69MB / 64MB
- 모바일 320px 이상 레이아웃과 입력 검증 유지
- `/Defense/` 프로덕션 빌드 성공
- 의존성 취약점 0건

### 브라우저 시각 검증 제한

Vite preview와 Chromium CDP를 실행했지만 현재 자동화 환경의 조직 정책이 localhost, 127.0.0.1, 컨테이너 IP와 file URL을 모두 차단했다. 따라서 실제 렌더링 스크린샷과 GPU 셰이더 컴파일 결과를 자동 브라우저로 확인했다고 주장하지 않는다. 빌드·정적 검증·GLB 파싱은 통과했으며, 최종 색감·그림자·림 셰이더와 터치 성능은 실제 iOS·Android 또는 일반 데스크톱 브라우저에서 확인해야 한다.

### 알려진 한계

- 세 NextGen GLB는 실제 파일이지만 스켈레탈 리깅과 내장 animation clip이 없는 파츠 기반 모델이다.
- 나머지 수호대 5종, 일반 요괴 3종, 보스 2종은 형태와 재질이 크게 개선된 절차형 NextGen 폴백이다.
- Normal·ORM·Emissive 전용 텍스처 세트와 KTX2 압축은 아직 적용하지 않았다.
- 텍스처 메모리가 63.69MB로 64MB 예산에 근접해 신규 대형 WebP 추가는 금지하고 KTX2 전환을 우선해야 한다.
- 실제 브라우저 셰이더 컴파일은 자동화 환경 정책으로 확인하지 못했다.

### 다음 패치 방향 — v3.1.0 / Engine 2.1.0

1. 불씨 깨비·장난 요괴·저승 호랑이 스켈레탈 리깅과 GLB animation clip
2. 나머지 캐릭터 10종 LOD0/LOD1 GLB 순차 교체
3. Base Color·Normal·ORM·Emissive 텍스처와 KTX2 전환
4. InstancedMesh 기반 장터 소품과 GPU 드로우콜 최적화
5. 보스 페이즈 전용 메시 morph 또는 교체 파츠
6. 전리품 장착 시 캐릭터 외형 장식 연결
7. 실제 모바일 GPU 스크린샷·프레임타임 회귀 기준 확보

### 다음 작업 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. 일반 브라우저에서 첫 실행과 WebGL 셰이더 컴파일 확인
5. 불씨 깨비·장난 요괴·저승 호랑이 전투와 도감 모델 확인
6. 데스크톱 그림자와 모바일 그림자 비활성 확인
7. 11방향 LOD 전환과 GLB 폴백 확인
8. 다음 버전은 게임 `3.1.0`, 엔진 `2.1.0`
9. 전체 ZIP과 변경분 패치 ZIP 두 개만 전달

## v3.1.0 / Engine 2.1.0 — SD 모바일 카툰 아트 바이블 잠금

### 변경 이유

v3.0.0의 스타일라이즈드 PBR 방향은 개별 자산의 품질을 높이는 데에는 유효했지만, 목표 게임의 캐주얼 운빨 디펜스 감성보다 비율과 재질이 무거웠다. 사용자 제공 참고안을 기준으로 대량 제작 전에 아트 방향을 다시 잠갔다.

### 고정된 방향

- 2.0~2.5등신, 목표 2.25등신
- 머리 44%, 몸통 34%, 다리 22%
- 큰 손·발과 둥근 모서리
- 4단 Toon Shading
- 청록 월광 Rim Light
- 모바일 Blob Shadow, 고품질 PCF Soft Shadow
- 대표색 1개, 보조색 1개, 발광색 1개
- 사실적 PBR·장신 비율·미세 장식 중심 디자인 금지

### 제작 순서

1. 불씨 깨비 골든 샘플과 7개 필수 모션
2. 장난 요괴·저주 무당·저승 호랑이
3. 바닥·신목·돌·장터 건물
4. 공용 UI 키트
5. 공용 VFX 키트
6. 승인된 템플릿 기반 대량 제작

### 5방향+미러링

신규 원거리 캡처는 0°, 45°, 90°, 135°, 180°의 5개 원본만 제작한다. 런타임은 상대각을 0~180°로 접고 좌측을 미러링해 기존 11방향 선택기와 호환한다. 기존 11프레임 WebP는 삭제하지 않고 호환 자산으로 유지한다.

### 코드·문서

- `src/art-style-tokens.js`
- `src/asset-specs.js`
- `src/engine/directional-impostor.js`
- `docs/ASSET_BIBLE.md`
- `docs/AI_ASSET_PROMPTS.md`
- `docs/BLENDER_EXPORT_GUIDE.md`
- `docs/PRODUCTION_ROADMAP.md`
- `scripts/verify-art-bible.mjs`

### 대표 GLB 교체

- `guardian-ember-sd-toon.glb`
- `monster-imp-sd-toon.glb`
- `boss-tiger-sd-toon.glb`

파일은 새 SD 비율로 재생성했으며 전투와 도감이 같은 슬롯을 사용한다. 아직 스켈레탈 리깅 자산은 아니고 파츠 기반 골든 샘플이다.

### 렌더링

- `MeshToonMaterial` + 4단 DataTexture Gradient
- `NeutralToneMapping`
- 월광 Rim Shader 유지
- 모바일 실시간 그림자 비활성, Blob Shadow 유지
- 데스크톱 PCF Soft Shadow 유지

### 다음 제작 작업

불씨 깨비를 Blender에서 실제 리깅하고 `idle`, `walk`, `run`, `attack`, `skill`, `hit`, `death` 클립을 납품한다. 해당 골든 샘플이 실기기에서 승인되기 전에는 나머지 캐릭터를 대량 생산하지 않는다.

---

## v3.2.0 / Engine 2.2.0 — 코드 무결성·런 수명 관리 패치

### 패치 목표

다음 단계의 스켈레탈 리깅, 대량 GLB 교체, 전리품 외형 장착을 시작하기 전에 코드 중복과 수명 오류가 누적되지 않도록 기반을 정리한다. 기능을 크게 늘리기보다 이벤트·타이머·애니메이션 상태·배포 파일의 단일 소유권을 명확히 하고, 같은 문제가 다시 들어오면 자동 검증에서 차단하도록 만든다.

### RuntimeLifecycle

신규 모듈 `src/runtime-lifecycle.js`:

- `EventRegistry`: 이벤트 등록·해제와 중복 키 검사
- `TaskScope`: 이름 있는 지연 작업 교체, 세대 취소, pending 진단
- `RuntimeLifecycle`: `ui`, `run`, `effects`, `system` 작업 영역 분리

런 수명 경계:

- `startRun()`에서 이전 런 작업과 효과 예약 취소
- `returnToTitle()`에서 런 세대 무효화
- `finishRun()`에서 남은 전투 예약 작업 취소
- 자동 웨이브, 무료 강림, 자동 합성, 보스 연속 패턴, 궁극기 연쇄 타격은 모두 런 토큰을 통과해야 실행
- 미션, 토스트, 콤보, 피격 섬광은 이름 있는 UI 작업으로 최신 요청만 유지

### 이벤트 중복 제거

- `bindUI()`는 두 번 호출되어도 재등록하지 않는 idempotent 구조
- 정적 UI 이벤트는 모두 `EventRegistry` 사용
- 유물·계약·축복·선택 강림·영구 성장 버튼은 생성할 때마다 리스너를 붙이지 않고 상위 컨테이너 이벤트 위임 사용
- 조이스틱, 카메라 드래그, 키보드, viewport resize도 같은 레지스트리에서 관리
- 동일 키 이벤트가 재등록되면 즉시 예외를 발생시켜 조용한 중복 입력을 방지

### 도감 뷰어 수명 정리

`src/codex-viewer.js`:

- 전용 `EventRegistry`
- 활성 상태 해제 시 `cancelAnimationFrame`
- 모델·임포스터·복제 텍스처 dispose
- WebGLRenderer dispose
- 중복 `setActive(true)` 시 RAF 루프 추가 생성 방지

### 골든 샘플 7모션 상태

`src/engine/animation-state-system.js`:

- `idle`, `move`, `run`, `attack`, `skill`, `hit`, `death`
- `setBaseState()` 추가
- Attack/Skill 원샷 중 입력 상태가 Walk에서 Run으로 바뀌면 종료 후 최신 Run으로 복귀
- Run은 보폭·상체 기울기·팔다리 진폭을 Walk와 분리
- Skill은 양팔·무기·대표 장식·월륜의 별도 충전 동작
- 11방향 LOD는 Run을 Move 아틀라스, Skill을 Attack 아틀라스로 안전 매핑

### 구버전 번들 제거

과거 빌드의 다음 파일 6개가 `public/assets`에 남아 매 빌드마다 `dist`로 복사되고 있었다.

- `index-B0uLkGTa.js`
- `index-C4HEqwCr.js`
- `index-DCYMisxj.js`
- `index-BpfmRvmR.css`
- `index-C2b85yCi.css`
- `index-yN890ryg.css`

모두 제거했으며 `prebuild`에서 `scripts/clean-obsolete-assets.mjs`를 자동 실행해 동일 패턴이 다시 들어오지 않게 했다. 실제 실행 번들은 `assets/game.js`와 코드 분할 chunk만 사용한다.

### 신규 자동 검증

- `scripts/verify-code-integrity.mjs`
  - 상대 import 해석
  - ES module 순환 의존성
  - DOM ID 중복
  - 메인 클래스 메서드 중복
  - main.js의 직접 setTimeout/setInterval 사용
  - EventRegistry 우회
  - 도감 dispose 경로
  - 구버전 Vite 해시 번들 잔존
- `scripts/verify-runtime-lifecycle.mjs`
  - 동일 이벤트 키 중복 차단
  - dispose 이후 이벤트 차단
  - 이름 있는 작업 최신 1개 유지
  - 세대 취소 이후 이전 작업 무효화
- `scripts/verify-golden-motion.mjs`
  - Walk 기본 상태
  - Attack 후 최신 Run 복귀
  - Skill 후 최신 Run 복귀
  - 7개 모션 상태
  - 컨트롤러 제거

### 최종 확인 항목

- 패키지 버전 `3.2.0`
- 엔진 버전 `2.2.0`
- 클린 `npm ci` 후 전체 `npm run verify` 재통과
- `/Defense/` 프로덕션 빌드와 번들 구문 검사 통과
- 기존 저장 키 변경 없음
- 전투 밸런스와 에셋 예산 변경 없음
- 텍스처 예산 `63.69MB / 64MB` 유지
- `npm audit`는 2026-07-21 검증 환경의 감사 API가 두 차례 HTTP 503을 반환해 결과를 확정하지 못함
- 다음 대형 래스터 추가 전 KTX2 전환 필요

### 다음 패치 방향 — v3.3.0 / Engine 2.3.0

1. 불씨 깨비 골든 샘플 스켈레탈 리그와 7개 실제 GLB AnimationClip
2. AnimationMixer 클립 우선, 절차형 모션 폴백 유지
3. LOD0/LOD1 공용 리그와 무기·장식 소켓
4. 5방향 원본 캡처 자동화 및 좌우 미러 검수 이미지
5. 텍스처 KTX2 전환으로 64MB 예산 여유 확보
6. 실제 모바일 브라우저 화면 기반 스크린샷 회귀 기준
7. main.js에서 도감·UI 상태·전투 연출의 단계적 모듈 분리

### 다음 작업 순서

1. `npm ci`
2. `npm run verify`
3. `VITE_BASE_PATH=/Defense/ npm run build`
4. 전체본에서 구형 `index-*.js/css` 미포함 확인
5. 같은 시드 연속 재시작 후 이전 보스 패턴·자동 합성 미발동 확인
6. 도감 반복 열기·닫기 후 RAF와 이벤트 수 증가 없음 확인
7. Attack·Skill 중 질주 입력 후 Run 복귀 확인
8. 다음 버전은 게임 `3.3.0`, 엔진 `2.3.0`
9. 전체 ZIP과 변경분 패치 ZIP 두 개만 전달



---

## v3.3.0 / Engine 2.3.0 — 전투 에셋 적용 가시성 패치

### 목표

대표 GLB 3종만 연결된 상태를 전체 아트 교체처럼 보이게 표현하지 않는다. 실제 전투 슬롯 전체를 GLB 카탈로그에 연결하고, 사용자가 현재 GLB가 로드됐는지 또는 절차형 폴백이 사용됐는지 게임 안에서 확인할 수 있게 한다.

### 실제 GLB 적용 범위

- 플레이어 1종: 대장 깨비
- 수호대 6종: ember, frost, wind, stone, bell, thunder
- 일반 요괴 4종: imp, runner, brute, shaman
- 보스 3종: tiger, serpent, king
- 총 14종, 전투와 도감에서 동일 자산 ID 사용

### 에셋 진단

카메라·조작 설정에 전투 에셋 진단 패널을 추가했다. 각 모델별로 다음을 표시한다.

- 준비됨: AssetPipeline 캐시에 GLB가 존재
- 폴백: GLB 로드 또는 인스턴스화 실패 후 절차형 모델 사용 횟수
- 생성: 누적 GLB 복제 인스턴스 수
- `3D 모델 고정`: 불씨 깨비·장난 요괴의 거리 기반 11방향 임포스터를 비활성화

### 코드 정리

- `createDokkaebiModel`, `createEnemyModel`, `createSacredTree`의 return 이후 죽은 구형 코드 제거
- 플레이어·수호대·요괴·보스 자산 ID 맵 단일화
- AssetPipeline 내부 폴백 Map 직접 접근 금지
- GLB 복제본의 공유 geometry 이중 dispose 방지
- 도감 뷰어도 동일 shared geometry 규칙 사용
- 코드 무결성 검증에서 죽은 모델 코드와 공유 geometry 수명 보호를 검사

### 필수 검증

1. 14개 GLB 모두 GLTFLoader 파싱
2. 각 GLB 필수 파츠 노드와 bounds 검사
3. 플레이어·수호대·몬스터·보스별 triangle 예산 검사
4. 타이틀 전시 수호대가 속성별 GLB를 사용하는지 확인
5. 설정창 진단 목록 14개와 `3D 모델 고정` 토글 확인
6. 폴백 사용 시 해당 자산 ID의 횟수만 증가하는지 확인
7. 유닛 제거·도감 종료 후 공유 geometry가 폐기되지 않는지 확인
8. 클린 설치, 전체 verify, `/Defense/` 빌드, dist 자산 14개 포함 확인

### 제한 사항

14종 모두 실제 GLB지만 아직 스켈레탈 본·스킨·내장 AnimationClip이 있는 최종 리깅 모델은 아니다. 시각 적용 범위와 런타임 교체 구조를 명확히 하는 단계이며, 다음 골든 샘플은 불씨 깨비 리깅과 7개 클립을 우선한다.


### v3.3.0 추가 검증 및 배포 안정화

- 모든 모델·임포스터·FX URL에 `ASSET_REVISION = 3.3.0` 쿼리를 적용해 같은 파일명의 구형 브라우저/CDN 캐시를 우회한다.
- `AssetPipeline`은 압축되지 않은 현재 GLB 때문에 DRACO 디코더를 부팅 시 불필요하게 preload하지 않는다. 향후 DRACO/KTX2 사용을 위해 Three.js 0.185.1 고정 CDN 디코더 경로는 명시한다.
- Firebase 전체 npm 패키지를 제거하고, 환경 변수가 설정된 경우에만 공식 gstatic 12.16.0 app/auth/firestore ESM을 지연 로드한다. 온라인 랭킹 기능은 유지한다.
- 패키지 저장소 장애 시에도 배포물을 만들 수 있도록 `npm run build:static`을 추가했다. 이 빌드는 고정 버전 Three.js import map, 상대 경로 모듈, 14개 GLB와 전체 자산을 포함한다.
- 정적 배포 검증은 진입점, import map, CSS, 상대 모듈 경로, v3.3.0 캐시 리비전, GLB 14종 존재를 검사한다.
- 2026-07-22 자동화 환경에서는 내부 npm tarball 저장소가 Vite 다운로드에 HTTP 503을 반환했다. 따라서 포함된 `dist/`는 `build:static`으로 생성했으며, 정상 레지스트리 환경에서는 Vite 번들 빌드를 추가로 권장한다.
- 오프라인 package-lock 감사 결과 취약점 0건. 실제 WebGL 화면 캡처는 CDN/DNS와 GPU 제한 때문에 완료했다고 주장하지 않는다.


## v3.4.0 / Engine 2.4.0 — AAA 캐주얼 SD 3D 스타일 잠금

- 사용자 제공 절대 프롬프트를 `DD-AAA-CASUAL-SD-PBR-3.0`으로 잠갔다.
- 목표 비율을 2.3등신, 렌더를 손그림 Stylized PBR·Soft AO·Subtle Rim으로 고정했다.
- 5방향 원본+미러링을 유지한다.
- 현재 14종 GLB는 Skin·AnimationClip·PBR 텍스처가 없는 개발용 프로토타입으로 격리했다.
- 게임 설정의 에셋 진단은 로딩 여부와 제작 승인 여부를 분리한다.
- 750종 프롬프트 카탈로그와 자동 스타일 잠금 검증을 추가했다.
- 기존 절차형 GLB 생성기는 `--prototype-only` 명시 없이는 실행되지 않도록 차단했다.
- 향후 첫 제작 대상은 도깨비 전사 골든 샘플 1종이며, 승인 전 대량 모델 생성은 금지한다.


## v3.5.0 / Engine 2.5.0 — 도깨비 전사 스켈레탈 골든 샘플

### 패치 목표

대량 에셋을 만들기 전에 공용 리그·필수 클립·소켓·PBR 맵·런타임 재생·자동 검증을 하나의 실제 GLB로 고정한다. 기술 통과와 최종 아트 승인을 분리해, 자동 생성 모델을 완성품으로 오인하는 경로를 막는다.

### 골든 샘플 결과

- 에셋 ID: `player-dokkaebi-warrior-golden-v1`
- 9,572 triangles / 기술 예산 6,000~10,000
- Skin 1개
- 공용 리그: `DOKKAEBI-HUMANOID-RIG-1`
- 7개 내장 AnimationClip: Idle, Walk, Run, Attack, Skill, Hit, Death
- 소켓: WeaponSocket, AccessorySocket
- 임베디드 맵: BaseColor, Normal, ORM, Emissive
- GLB extras에 스타일 잠금·기술 준비·아트 리뷰 단계 기록
- 기존 플레이어 프로토타입 GLB 제거

### 런타임 연결

- 전투와 도감은 동일 골든 GLB를 사용한다.
- AnimationClip이 있으면 Three.js AnimationMixer를 우선 사용한다.
- 절차형 파츠 애니메이션은 클립이 없는 프로토타입과 로딩 실패 폴백에서만 사용한다.
- `art-review`와 `production-approved` 자산은 작가가 만든 스타일라이즈드 PBR 재질을 보존한다.
- 무기·장식 소켓은 공용 별칭으로 해석한다.
- GLB 인스턴스에 애니메이션·기술 측정값·승인 상태를 전달한다.

### 사용자 검수 UI

카메라·조작 설정에 골든 샘플 점수 카드와 `7모션 검수` 버튼을 추가했다. 기존 도감 3D 뷰어를 중첩 모달로 열어 대기·걷기·달리기·공격·기술·피격·사망을 직접 전환할 수 있다.

에셋 진단은 다음 상태를 분리한다.

- GLB 로드 성공
- 기술 검수 통과 및 아트 리뷰 대기
- 최종 제작 승인
- 개발용 프로토타입 또는 폴백

### 코드 구조와 중복 방지

- `src/golden-sample-spec.js`: 에셋 ID, 리그, 클립, 소켓, 맵, 예산의 단일 원본
- `src/asset-diagnostics.js`: 진단 요약·HTML 렌더링과 이스케이프
- `scripts/generate_golden_hero_glb.py`: 기술 골든 GLB 생성기
- `scripts/verify-golden-sample-glb.mjs`: GLB 바이너리 구조 검사
- `scripts/verify-asset-runtime.mjs`: 로더·Mixer·재질·소켓·UI 회귀 검사
- 실패한 GLB 프리로드에서 진행률이 두 번 증가하던 버그 수정
- 압축 선언이 있는 에셋에만 DRACO/KTX2 디코더 준비

### 문서

- `docs/GOLDEN_SAMPLE_PRODUCTION_SPEC.md`
- `docs/GOLDEN_SAMPLE_CAPTURE_MANIFEST.json`
- `docs/CODE_ARCHITECTURE_v3.5.md`
- `docs/CURRENT_ASSET_AUDIT.md`
- `docs/ASSET_BIBLE.md`

### 검증 기준

- 상대 import 해석, 순환 의존, 중복 DOM ID, 중복 메서드
- 직접 타이머·직접 이벤트 우회
- 런 수명·이벤트 중복·골든 모션 회귀
- GLB triangle, Skin, 7클립, 공용 노드, 소켓, 4개 텍스처
- 모든 mesh primitive의 JOINTS_0·WEIGHTS_0
- art-review 재질 보존과 AnimationMixer 우선 경로
- 도감 7모션 UI와 정리 경로
- 정적 ESM `/Defense/` 배포 구조

### 승인 상태와 제한

현재 골든 샘플은 **기술 검수 통과 / 아트 디렉터 리뷰 대기**다. 실제 모바일 GPU에서 실루엣, 얼굴, 손그림 재질, 림 라이트, 모션 타이밍을 검수하기 전에는 완성 에셋으로 표시하지 않는다. 돌갑옷 귀수와 저주 무당도 기술 검수 통과 / 아트 리뷰 대기로 승격했다. 나머지 11종 전투 GLB는 개발용 프로토타입으로 유지한다.

자동화 환경에서 npm 패키지 다운로드가 타임아웃되어 일반 Vite 번들을 최종 생성하지 못했다. 전체본의 `dist/`는 고정 Three.js 0.185.1 import map을 사용하는 정적 ESM 방식으로 생성하고 진입점·모듈 경로·GLB 14종·자산 리비전을 검증했다.

### 다음 안전한 확장 순서

1. 실기기에서 도깨비 전사 골든 샘플 아트 리뷰
2. 미승인 항목 수정 후 `production-approved` 승격
3. 동일 리그로 근접 적·원거리 적·첫 보스 3종 제작
4. 무기·장식 교체 소켓과 전리품 외형 연결
5. KTX2/LOD1 적용과 5방향 캡처 자동화
6. 승인된 템플릿만 사용해 수호대·몬스터를 단계적으로 확장


## v3.6.0 / Engine 2.6.0 — Moonstone HUD·모바일 겹침 방지

### 핵심 변경

- `src/ui-layout-manager.js` 추가. 기존 고정 좌표 HUD를 상단 상황, 중앙 게이지, 좌측 정보, 우측 수호대 레일로 런타임 재배치한다.
- 화면 크기와 보스 상태에 따라 `auto/full/minimal` 밀도를 적용한다.
- 표시 중인 주요 레일과 하단 조작의 실제 사각형을 측정해 겹침 쌍을 기록한다. 성능 로그 JSON의 `diagnostics.hudLayout`에서 프로필, 모드, 충돌 쌍을 확인할 수 있다.
- 5개 액션 버튼이 4열 그리드에 들어가던 구조 오류를 수정했다. 데스크톱은 5열, 모바일은 3+2 배열이다.
- 320px에서 조이스틱과 액션 도크 사이 최소 26px를 확보한다.
- 보스전에는 좌우 정보 레일을 보스 체력바 아래로 자동 이동한다.
- `ui-overflow-safe`에서는 시드와 설명 같은 저우선 정보만 축약한다.
- 우측 상단 HUD 밀도 버튼과 설정창의 `HUD 자동 정리`를 추가했다.
- 제목 화면, 모달, 패널, 버튼을 Moonstone HUD 토큰으로 통일했다.
- `CHARACTER_ASSET_LABELS`의 중복 보스 키를 제거했다.

### 검증

- `npm run verify` 전체 통과
- 상대 import 35개, 순환 의존 0건
- DOM ID 192개, 중복 0건
- 메인 클래스 메서드 253개, 중복 0건
- 직접 이벤트·직접 지연 타이머 우회 0건
- `scripts/verify-ui-v360.mjs` 추가
- 320px 하단 조작 수평 안전 간격 26px
- 상단 HUD→상황 레일→게이지→보스 체력 최소 간격 검증
- 기존 GLB 14종, 골든 샘플 Skin·7 Clip, 도감·약점·전리품 회귀 검증 통과
- 텍스처 예산 63.69MB / 64MB 유지
- 정적 ESM `dist/` 생성 및 14종 GLB 포함 검증

### 빌드 환경 제한

2026-07-22 검증 환경에서 `npm ci`가 Vite/Three 패키지 본문을 완전히 받지 못해 일반 Vite 실행 파일을 만들지 못했다. 소스 전체 검증은 통과했고, 포함된 `dist/`는 `npm run build:static`으로 생성해 고정 Three.js 0.185.1 import map, 상대 경로, 자산 리비전 3.6.0, GLB 14종을 검사했다. Chromium 스크린샷은 DBus/GPU 초기화 단계에서 종료되지 않아 생성하지 못했다.

### 다음 권장 순서

1. 실제 320×568, 390×844 iOS/Android에서 HUD 겹침 로그 확인
2. 긴 한국어 현지화 문자열로 카드 폭 스트레스 테스트
3. 골든 샘플 아트 리뷰 수정
4. 승인된 공용 리그 기반 근접·원거리 적 제작
5. KTX2로 텍스처 예산 여유 확보


## v3.7.5 / Engine 2.7.3 — 빌드 계약·공용 리그 적·UI 스트레스 안정화

### GitHub Actions 빌드 오류 수정

- Rolldown의 `[MISSING_EXPORT] BOSS_ASSET_IDS` 실패를 확인했다.
- `main.js`가 에셋 ID를 `src/engine/index.js` 배럴에서 가져오지 않고 `src/engine/asset-catalog.js` 단일 원본에서 직접 가져오도록 변경했다.
- `scripts/verify-module-exports.mjs`를 추가해 모든 상대 named import와 대상 export를 `npm run verify`에서 확인한다.
- 동일 유형의 누락은 Vite 이전 검증 단계에서 실패한다.
- `src/engine/index.js`의 재수출은 편의 API로 유지하지만 메인 게임 빌드의 필수 의존점이 아니다.

### 공용 리그 적 2종

- `monster-brute-sd-toon.glb`: 8,396 triangles, Skin 1, 7 AnimationClip, 공용 소켓, PBR 맵 4종.
- `monster-shaman-sd-toon.glb`: 7,712 triangles, Skin 1, 7 AnimationClip, 공용 소켓, PBR 맵 4종.
- 두 모델 모두 `DOKKAEBI-HUMANOID-RIG-1`을 사용한다.
- 승인 상태는 `art-review`; production-approved는 아니다.
- 현재 상태: 기술 리뷰 3종, 프로토타입 11종, 최종 승인 0종.
- `scripts/generate_rigged_enemy_candidates.py`와 `scripts/verify-rigged-enemy-candidates.mjs` 추가.

### UI 스트레스 계약

- `src/ui-layout-contract.js`를 추가했다. 폭·높이·글자 배율·보스 상태를 단일 입력으로 받아 HUD 폭과 조작 간격을 계산한다.
- 필수 프로필: 320×568, 360×640, 390×844, 430×932, 568×320, 360×640 큰 글자 122%.
- `ResizeObserver`로 카드 크기 변화를 감지한다.
- 긴 한국어 문구는 `scrollWidth/clientWidth`로 overflow를 감지한다.
- `ui-copy-tight`, `ui-emergency-layout`은 저우선 정보부터 축약하며 핵심 전투 정보와 조작은 유지한다.
- 모달은 visual viewport 안에서 스크롤되며 짧은 화면 밖으로 벗어나지 않는다.
- `scripts/verify-ui-v370.mjs` 추가.

### 텍스처 최적화

- 불씨 깨비·장난 요괴의 상태별 11방향 아틀라스 6개를 1024×768에서 768×576으로 조정했다.
- 각 셀은 192×192이며 원거리 LOD 가독성을 유지한다.
- 정적 텍스처 예산은 63.69MB에서 53.19MB로 감소했다.

### 검증 상태

- `npm run verify` 전체 통과.
- 상대 모듈 37개, 순환 의존 0건.
- DOM ID 192개, 중복 0건.
- 메인 클래스 메서드 253개, 중복 0건.
- 직접 이벤트·직접 지연 타이머 우회 0건.
- 골든 영웅과 공용 리그 적 2종의 Skin·7클립·소켓·PBR 맵 검사 통과.
- 6개 UI 스트레스 프로필 검사 통과.
- 정적 ESM `dist/` 생성 및 GLB 14종 포함 검사 통과.

### 빌드 환경 메모

현재 자동화 컨테이너는 `registry.npmjs.org` DNS를 해석하지 못해 `npm ci`와 실제 Vite 실행을 완료하지 못했다. 소스의 named import/export 계약과 정적 배포는 검증했으며, GitHub Actions에서는 다음 순서를 사용한다.

```bash
npm ci
npm run verify
npm run build
```

사용자가 보고한 `BOSS_ASSET_IDS` 오류 경로는 직접 import로 제거됐다.


## v3.7.2 / Engine 2.7.3 — CI 실패 원인 가시화 핫픽스

### 배경

GitHub Actions에서 `npm run verify`가 exit 1로 종료됐지만 로그 마지막에는 PASS 항목만 남아 실제 초기 FAIL이 가려졌다. 전체본 자체는 동일 환경에서 exit 0으로 통과했다.

### 변경

- `scripts/verify-project.mjs`의 중간 `process.exit(1)` 세 곳 제거.
- 프로젝트 검사를 끝까지 수행한 뒤 마지막에 한 번만 종료.
- 실패 목록을 `VERIFY FAILURE DIGEST`로 번호와 함께 재출력.
- GitHub `::error title=Project verification failed` 주석 추가.
- GitHub Pages 워크플로가 `verify.log`를 보존하고 실패 요약을 마지막에 다시 출력.
- 추가 루트 Markdown은 런타임 무관 파일이므로 INFO 처리.
- SVG 정책은 `public/`, `src/` 런타임 경로만 검사.
- 구형 Vite 해시 번들을 동적 정규식으로 검사.
- 게임 버전 비교는 `package.json`을 단일 기준으로 사용.
- `scripts/verify-ci-reporting.mjs` 회귀 검사 추가.

### 검증

- 정상 전체 검증 exit 0.
- 타이틀 버전을 의도적으로 손상시킨 실패 테스트 exit 1.
- 실패 테스트에서 로그 마지막에 정확한 원인과 GitHub 오류 주석 출력.
- 정적 ESM `/Defense/` 배포본 생성 및 14개 GLB 포함 검사 통과.

### 환경 메모

현재 컨테이너에는 Vite 실행 파일이 설치되지 않아 실제 Vite 번들은 로컬에서 생성하지 못했다. GitHub Actions는 `npm ci` 후 실제 Vite 빌드를 수행하며, 이번 변경은 그 이전 검증 실패 원인을 명확히 표시하고 비실행 파일 때문에 배포가 차단되는 조건을 제거한다.

## v3.7.3 / Engine 2.7.3 — Absolute No-SVG CI Hotfix

- `scripts/svg-policy.mjs`를 단일 SVG 정책 검사기로 추가했다.
- 실제 SVG 파일, `.svg` 런타임 참조, 인라인 `<svg>`, SVG data URI/MIME을 금지한다.
- 기존 검증기의 빈 위반 메시지 오탐을 제거했다.
- `preverify`와 `prebuild` 청소가 저장소에 남은 모든 `.svg` 파일을 재귀적으로 제거한다.
- `verify-svg-policy.mjs`가 정책 문자열 오탐과 실제 참조 누락을 회귀 검사한다.


## v3.7.4 / Engine 2.7.3 — PWA Manifest PNG Migration Hotfix

- 기존 저장소의 `public/manifest.webmanifest`에 남은 `icon.svg`와 `image/svg+xml`을 PNG 3종으로 자동 마이그레이션한다.
- `preverify`와 `prebuild`에서 동일한 멱등 마이그레이션을 실행한다.
- SVG 오염 manifest 회귀 테스트를 통과했다.

## v3.7.5 / Engine 2.7.3 — Runtime Asset Recovery Hotfix

### 원인

- v3.7.4 전체본에는 전투 GLB 14종이 있었지만 패치 ZIP에는 GLB가 0개였다.
- 이미 파일이 누락된 Git 저장소에 패치를 덮어써도 `guardian-frost-sd-toon.glb` 같은 파일이 복구되지 않았다.
- `verify-asset-readiness.mjs`가 `readFileSync`에서 즉시 예외를 던져 나머지 누락 파일을 확인할 수 없었다.

### 변경

- v3.7.5 패치 ZIP에 전투 GLB 14종 전부를 강제 포함한다.
- `docs/RUNTIME_ASSET_INVENTORY_v3.7.5.json`에 정확한 바이트 크기와 SHA-256을 기록한다.
- `scripts/verify-required-runtime-assets.mjs`가 존재 여부, Git LFS 포인터, GLB 2 헤더, 선언 길이, 파일 크기, SHA-256을 검증한다.
- 누락·손상 항목은 GitHub `::error`와 `RUNTIME ASSET FAILURE DIGEST`로 모두 출력한다.
- 기존 에셋 준비 검증도 누락 파일에서 예외로 죽지 않고 실패 목록을 누적한다.

### 재현 검증

- `guardian-frost-sd-toon.glb`를 의도적으로 제거했을 때 exit 1과 정확한 파일명·복구 안내가 출력됐다.
- 파일 복원 후 14/14 크기·헤더·해시 검증 통과.
- `npm run verify` 전체 통과.
- 정적 ESM `dist/` 생성 후 전투 GLB 14종 포함 검사 통과.


## v3.8.0 / Engine 2.8.0 — IP Production Foundation

### 사용자 기준 통합

- 프로젝트를 13개 제작 영역으로 분리했다: ArtBible, Characters, Monsters, Boss, UI, Icons, VFX, Map, Sound, Animation, Unity, Monetization, LiveOps.
- 한국 전통 판타지의 도깨비·신선·무당·저승사자·구미호·호랑이·산신령·용을 핵심 IP 모티프로 고정했다.
- 2.3등신, 머리 42%, 몸 35%, 다리 23%, 큰 눈·둥근 손·큰 무기 규격을 명시했다.
- Gold, Blue, Purple, Red, White를 주색으로 사용하고 회색·어두운 갈색·칙칙한 저채도는 지배색으로 금지했다.

### 5대 기준 문서

- 게임 기획서
- 아트 바이블
- UI/UX 설계서
- 애니메이션 바이블
- 에셋 제작 마스터리스트

모든 문서는 `production/DokkaebiDefense`에 있으며 `docs/IP_PRODUCTION_INDEX_v3.8.0.md`가 진입점이다.

### 에셋 마스터리스트

사용자가 제시한 표의 정확한 합계는 1,130개다.

- 캐릭터 50
- 몬스터 60
- 보스 20
- 무기 100
- 스킬 아이콘 120
- UI 250
- VFX 180
- 타일 70
- 배경 30
- 오브젝트 250

각 항목은 고유 ID, 한·영 이름, 제작 폴더, 납품 파일명, 포맷, 우선순위, 등급, 마일스톤, 기술 규격, 승인 상태를 가진다. 초기 상태는 모두 planned이며 아트·기술 이중 승인 전 approved 사용을 금지한다.

### 자동화

- `scripts/generate-ip-production-masterlist.mjs`가 1,130개 목록과 카테고리별 카탈로그를 결정적으로 생성한다.
- `scripts/verify-ip-production-v380.mjs`가 13개 폴더, 5대 문서, 수량, 중복 ID·파일명, 포맷, 11직업, 7등급, 7스테이지, 99개 모션 행을 검사한다.
- `npm run verify`에 IP 제작 계약을 포함했다.
- 기존 절대 SVG 금지, PNG manifest, 전투 GLB 14종 해시 검증은 유지한다.

### 런타임 영향

이번 패치는 제작 운영 레이어를 추가하는 기반 업데이트다. 1,130개 계획 항목을 실제 완성 에셋으로 과장하지 않으며, 현재 런타임 에셋의 품질 감사 상태도 변경하지 않는다.


## v4.0.0 / Engine 2.9.0 — P0 Runtime Class & Enemy Expansion

### 실제 플레이 확장

- 타이틀에서 도깨비 전사·궁수·법사 3개 시작 직업을 선택한다.
- 전사는 균형형 근접/폭발, 궁수는 장거리 관통과 다중 화살, 법사는 광역 피해와 둔화를 사용한다.
- 선택 직업은 런 저장소에 보존되고 전투 시작 능력치·기본 공격·기술·모델 에셋에 반영된다.
- 유물 선택 후 `WeaponSocket`과 `AccessorySocket`에 무기·관·후광 런타임 키트를 다시 장착한다.

### 전투 후보 에셋

- 플레이어 후보: `player-dokkaebi-archer-candidate-v1`, `player-dokkaebi-mage-candidate-v1`.
- 일반 적 후보: `monster-ghost-candidate-v1`, `monster-skeleton-candidate-v1`, `monster-crow-candidate-v1`.
- 다섯 모델은 공용 Skin, Idle/Walk/Run/Attack/Skill/Hit/Death 7개 클립, 무기·장식 소켓을 포함한다.
- 기존 공용 리그 메시에서 파생한 기술 후보이며 최종 전용 조형·표정·손그림 텍스처 승인은 대기 상태다.
- 현재 전투 에셋 상태는 기술 리뷰 8종, 프로토타입 11종, 최종 제작 승인 0종이다.

### 도감·HUD·래스터 UI

- 도감 32종, 약점 연구 10종, 전리품 13종으로 확장했다.
- 보스 의도를 타격·소환·제어로 분류하고 PNG 아이콘, 유형 라벨, 남은 시간, 진행 게이지, 위험 단계로 표시한다.
- 직업 3종·신규 적 3종·보스 의도 3종의 256×256 PNG 아이콘을 추가했다.
- SVG 파일·경로·인라인·MIME·data URI 금지 정책은 그대로 유지한다.

### 검증 계약

- 전투 GLB 19종을 `docs/RUNTIME_ASSET_INVENTORY_v4.0.0.json`의 크기·GLB2 헤더·SHA-256으로 검증한다.
- 저비용 프로토타입 적은 3.2k, 공용 리그 기술 후보 적은 9k triangles 예산으로 분리한다.
- `scripts/verify-p0-v390.mjs`가 후보 GLB 5종, PNG 9종, 직업 분기, 유물 소켓, 보스 HUD를 검사한다.
- `npm run verify` 전체 회귀 검증을 통과해야 빌드가 진행된다.
