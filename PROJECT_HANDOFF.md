# PROJECT HANDOFF — 도깨비 운빨 수호대 3D

이 문서는 대화가 끊기거나 다른 작업자가 이어받아도 프로젝트를 계속 개발할 수 있도록 모든 핵심 기록을 누적하는 단일 인수인계 파일입니다.

- 마지막 갱신: 2026-07-20
- 현재 버전: `1.3.0`
- 프로젝트 폴더: `DokkaebiLuckDefense3D_v1.3.0`
- 현재 패치명: 5성 궁극 진화 & 초행 임무

---

## 1. 사용자 요구와 산출물 규칙

사용자는 AAA급 사실성보다는 모바일에서 가볍게 돌아가면서도 첫인상과 성장 연출에서 감탄이 나오는 3D 웹게임을 원합니다.

선호 감성:

- 도깨비 키우기류의 수집과 성장
- 운빨 디펜스류의 랜덤 소환, 합성, 잭팟
- 플레이어가 3D 공간을 직접 돌아다니는 액션
- 짧은 판 안에서 크게 성장하는 피드백

반드시 유지할 전달 방식:

1. 매 패치마다 전체 프로젝트 ZIP 제공
2. 매 패치마다 변경 파일만 포함한 패치 ZIP 제공
3. `PROJECT_HANDOFF.md`를 매 패치 누적 갱신
4. 다운로드 파일은 답변 최하단 배치
5. 파일 아래에 다음 패치 예상 라인 표시
6. ZIP에 `node_modules`, `.firebase`, 로그 파일 제외
7. 전체 ZIP에는 미리 빌드된 `dist/` 포함
8. 패치 ZIP의 `PATCH_FILES/` 내용만 직전 버전 프로젝트 루트에 덮어쓴다.
9. 삭제 파일은 패치 ZIP의 `DELETE_FILES.txt`에 기록한다.
10. `APPLY_PATCH.txt`, `DELETE_FILES.txt`, `PATCH_MANIFEST.json`은 패치 ZIP 바깥 안내 영역에 두고 프로젝트로 복사하지 않는다.
11. 프로젝트 루트 Markdown은 `README.md`, `PROJECT_HANDOFF.md` 두 개만 유지한다.

---

## 2. 기존 Defense 프로젝트에서 얻은 교훈

기존 `Defense.zip`은 기능 부족이 아니라 중심 재미를 검증하기 전에 에셋, UI, 패치, 폴백 시스템이 과도하게 누적된 것이 문제였습니다.

분석 당시 주요 상태:

- 전체 약 550MB
- node_modules 제외 약 249MB
- 이미지 약 3,300개
- 버전명이 붙은 에셋 약 2,885개
- 소스 약 37,700줄
- 대형 단일 씬 파일과 여러 미술 방향 공존

재사용한 개념:

- 웨이브와 적 데이터 구조
- 로컬 저장 우선 방식
- Firebase 익명 로그인과 랭킹
- 모바일 저사양 대응

버린 것:

- Phaser 렌더링과 기존 화면 구조
- 대량 이미지 에셋
- 복잡한 로그인, 월드맵, 상점, 성장 메뉴
- 여러 아트 스타일과 복구 시스템의 중복

개발 원칙:

> 메뉴와 콘텐츠 양보다 한 판의 코어 재미를 먼저 완성한다.

---

## 3. 게임 한 줄 정의

3D 달빛 야시장을 직접 뛰어다니며 엽전을 줍고, 랜덤 도깨비를 소환·합성해 사방에서 몰려오는 요괴를 막는 8~10분 액션 운빨 디펜스.

핵심 루프:

`이동 → 자동 공격 → 엽전 직접 수집 → 랜덤 소환 → 3개 자동 합성 → 인연 완성 → 축복 선택 → 보스 격파`

절대 흔들지 않을 요소:

- 플레이어가 직접 움직인다.
- 골드는 바닥에 떨어지고 직접 주워야 한다.
- 소환은 랜덤이며 나쁜 운은 대박 기운으로 누적된다.
- 동일 종류·동일 별 3개는 자동 합성된다.
- 짧은 전투 판의 밀도를 복잡한 메뉴보다 우선한다.

---

## 4. 기술 스택

- Three.js `0.185.1`
- Firebase Web SDK `12.16.0`
- Vite `8.1.5`
- Vanilla JavaScript ES modules
- HTML/CSS HUD
- Web Audio API 절차형 효과음
- PWA manifest + service worker
- Firebase 익명 로그인 + Firestore 랭킹
- GitHub Actions + GitHub Pages

Node 권장:

- Node.js `20.19+` 또는 `22.12+`

명령:

```bash
npm install
npm run verify
npm run dev
npm run build
npm run preview
npm run deploy:rules
npm run deploy
```

---

## 5. 연결된 GitHub와 Firebase

### GitHub

기존 Defense 문서에서 확인한 Pages 주소:

- `https://junl-im.github.io/Defense/`

예상 저장소:

- `https://github.com/junl-im/Defense`

주의:

- 제공된 ZIP에는 `.git`이 없어 저장소 주소는 Pages 주소를 바탕으로 역추론함
- GitHub Desktop에 표시되는 실제 origin이 다르면 실제 origin을 우선
- Pages base path는 `/Defense/`
- 워크플로: `.github/workflows/deploy.yml`

### Firebase

기존 Defense 설정 파일에서 직접 확인:

- 프로젝트 ID: `web-game2`
- Auth Domain: `web-game2.firebaseapp.com`
- Storage Bucket: `web-game2.firebasestorage.app`
- 연결 파일: `.env.production`, `.firebaserc`

새 게임 랭킹 컬렉션:

- `dokkaebiScores`

기존 Defense에서 사용하는 Firestore 경로:

- `users`
- `leaderboards/{boardId}/scores/{uid}`
- `dailySeeds`

`firestore.rules`는 기존 경로와 `dokkaebiScores`를 모두 보존하는 병합 규칙입니다. 새 게임 규칙만 단독 배포하면 기존 Defense 기능이 깨질 수 있습니다.

권장 배포:

- 정적 게임: GitHub Pages
- 익명 로그인과 랭킹: Firebase
- Firebase Hosting은 기존 콘텐츠 교체 가능성이 있으므로 선택 사용

---

## 6. 현재 구현 상태

### 전장과 조작

- 달빛 야시장 단일 3D 전장
- 중앙 신목과 사방 요괴문
- 절차형 등불, 시장 좌판, 달, 바위, 도깨비불
- 모바일 조이스틱 이동
- 오른쪽 드래그 카메라
- PC 키보드 조작
- 플레이어 자동 기본 공격
- 질주
- 도깨비불 난무 광역기
- 적 처치 후 엽전 직접 수집

### 도깨비 6종

- 불씨 깨비: 연속 공격
- 달서리 깨비: 둔화
- 바람 갓깨비: 장거리 관통
- 바위 몽둥깨비: 범위 공격
- 방울 무당깨비: 연쇄 공격
- 번개 장군깨비: 처형

### 성장과 운빨

- 1성~5성
- 동일 종류·동일 별 3개 자동 합성
- 별에 따라 크기, 뿔, 오라, 발광, 투사체 강화
- 슬롯 15개
- 슬롯이 가득 차면 가장 약한 도깨비 환생
- 랜덤 소환
- 낮은 등급 소환 시 대박 기운 증가
- 대박 기운 100%에서 영웅 이상 보장
- 인연 시너지 6종
- 달의 축복 9종

### 적과 웨이브

- 일반 적 4종
- 보스 2종
- 총 10웨이브
- 웨이브 5: 저승 호랑이
- 웨이브 10: 백귀 야행왕

### 전투 피드백

- 첫 2성 도깨비 무료 강림
- 첫 웨이브 자동 시작
- 미션 시네마틱 배너
- 보스 전용 체력바
- 연속 처치 체인
- 10연속 처치 엽전 보너스
- 치명타와 3D 전투 숫자
- 무결점 웨이브 보너스
- 모바일 진동
- 평균 FPS 기반 동적 해상도 조절

### v1.3 궁극 진화와 초행 임무

- 5성 도깨비 6종 고유 자동 궁극기
- 5성 탄생 시 약 2.25초 카메라 집중 연출
- 전용 신화 각성 배너, 광주, 파티클, 진동
- 보유 목록에 합성 `1/3`, `2/3` 진행 표시
- 첫 플레이 전용 임무 3단계
- 임무 완료 상태를 로컬 저장하여 이후 판에서는 숨김
- 게임 데이터와 SoundEngine 최소 단위 모듈 분리

---

## 7. v1.2.0 변경 기록

목표:

- 중앙으로 걷기만 하던 적에게 피하고 유도하고 측면을 잡는 판단을 추가
- 랜덤 소환의 재미를 유지하면서 제한된 선택권 제공
- 프로젝트 문서와 패치 파일 누적을 정리

구현 내용:

### 두억 질주꾼

- 일정 거리에서 이동 정지
- 0.82초 동안 붉은 돌진 경로 표시
- 신목 방향으로 고속 돌진
- 플레이어와 충돌하면 밀쳐내고 짧은 혼절
- 신목에 도달하면 기본 공격보다 큰 피해

### 저주 무당

- 플레이어 현재 위치에 보랏빛 장판 예고
- 예고 중 플레이어가 수호대에서 멀어져 장판을 외곽으로 유도 가능
- 활성 장판 안 도깨비 공격 주기 1.85배 증가
- 장판 지속 5.2초

### 돌갑옷 귀수

- 전방에 실제 3D 방패 모델 추가
- 방패 정면에서 들어오는 투사체 피해 65% 감소
- 방패 피격 시 발광과 `방패!` 전투 텍스트
- 플레이어가 측면 또는 후방으로 이동하도록 유도

### 보스 충격파

- 기존 즉발 포효를 1.08초 예고 링 공격으로 변경
- 범위 안 플레이어는 넉백, 1.05초 혼절, 궁극기 재사용 시간 +1.5초
- 범위 밖 회피 시 점수 +120

### 삼지선다 소환

- `삼지선다 부적` 축복 추가
- 웨이브 3 첫 축복 후보에 반드시 포함
- 선택 시 소환권 2개 획득
- 소환 등급은 기존 확률로 먼저 결정
- 같은 등급의 서로 다른 도깨비 3종 중 하나 선택
- 비용, 대박 기운, 자동 합성 구조는 기존 시스템 유지

### 파일 정리

- 루트 Markdown을 `README.md`, `PROJECT_HANDOFF.md` 두 개로 제한
- 이전 `GAME_DESIGN`, `PATCH_HISTORY`, `QA_CHECKLIST`, 패치 노트 등은 단일 인수인계 문서에 통합
- 패치 안내는 패치 ZIP의 TXT 파일로만 제공
- 검증 스크립트에서 루트 Markdown 개수를 검사

---

## 7-A. v1.3.0 변경 기록

목표:

- 5성을 단순 배수 상승이 아니라 한 판의 기억에 남는 사건으로 만들기
- 첫 플레이에서 소환과 합성 목표를 명확히 제시하기
- 큰 단일 파일의 정적 데이터와 오디오를 안전하게 분리하기

구현 내용:

### 5성 고유 궁극기

- 불씨 깨비 `적련 백화`: 목표 주변 반경 5.4 광역 화염
- 달서리 깨비 `월빙 결계`: 반경 10.5 피해와 4.8초 강력 둔화
- 바람 갓깨비 `천풍 만발`: 사거리 14.5 안 최대 12개 목표 공격
- 바위 몽둥깨비 `태산 붕괴`: 반경 4.8 고위력 피해와 일반 적 밀쳐내기
- 방울 무당깨비 `백귀 방울춤`: 사거리 13 안 최대 10연쇄
- 번개 장군깨비 `천뢰 심판`: 보스 우선 낙뢰, 일반 적 34% 이하 및 보스 12% 이하 처형
- 궁극기 재사용 시간은 종류별 13.5~18초

### 5성 탄생 연출

- 5성 직접 소환 또는 자동 합성 시 공통 실행
- 카메라가 해당 도깨비를 약 2.25초간 회전하며 집중
- 전투 시간은 연출 중 약 42% 속도로 감소
- 전용 배너에 도깨비 이름과 궁극기 이름 표시
- 다중 링, 신화색 파티클, 화면 흔들림, 진동과 전용 음향
- 연출 직후 첫 궁극기는 약 1.6초 뒤 사용 가능

### 합성 진행 표시

- 우측 도깨비 목록에 같은 종류·같은 별 보유 수를 `합성 1/3`, `합성 2/3`으로 표시
- 5성은 합성 진행 대신 `궁극 · 궁극기명` 표시

### 첫 플레이 임무

1. 도깨비 3회 강림: 35 엽전
2. 첫 자동 합성: 45 엽전
3. 저승 호랑이 격파: 80 엽전 + 삼지선다 선택권 1개

- 세 임무를 모두 완료하면 `dokkaebi-first-missions-complete` 로컬 키 저장
- 완료한 사용자의 이후 플레이에는 임무 패널을 표시하지 않음

### 모듈 분리

- `src/game-data.js`: 등급, 도깨비, 적, 시너지, 축복 데이터
- `src/sound-engine.js`: Web Audio 효과음 엔진
- `src/main.js`: 런타임 게임 로직과 Three.js 월드 유지

---

## 8. 주요 밸런스 값

파일: `src/main.js`

- 시작 골드: 70
- 첫 무료 소환: 2성 보장
- 기본 소환 비용: 30
- 4회 소환마다 비용 +5
- 최소 소환 비용: 18
- 최대 웨이브: 10
- 신목 체력: 100
- 플레이어 기본 사거리: 8.8
- 질주 재사용: 4.2초
- 광역기 재사용: 13초
- 체인 유지: 1.85초
- 무피해 웨이브 추가 보너스: `10 + wave × 2`

기본 소환 확률:

- 4성 약 1.8%
- 3성 약 8.7%
- 2성 약 18.5%
- 나머지 1성
- 대박 기운 100%: 3성 보장, 12% 확률로 4성

v1.2 패턴 값:

- 질주 경고: 0.82초
- 질주 속도: 11.5
- 질주 신목 피해 배율: 1.65
- 저주 예고: 0.82초
- 저주 지속: 5.2초
- 저주 공격 주기 배율: 1.85
- 방패 정면 피해 배율: 0.35
- 보스 충격파 예고: 1.08초
- 보스 충격파 반경: 5.5
- 삼지선다 소환권: 2회

v1.3 값:

- 신화 연출: 2.25초
- 연출 중 게임 시간 배율: 0.42
- 불씨 궁극 쿨타임: 13.5초
- 달서리 궁극 쿨타임: 16초
- 바람 궁극 쿨타임: 14.5초
- 바위 궁극 쿨타임: 17.5초
- 방울 궁극 쿨타임: 15초
- 번개 궁극 쿨타임: 18초
- 초행 임무 보상: 35 / 45 / 80 엽전
- 초행 최종 보상: 삼지선다 선택권 1개

실제 Android/iPhone 플레이 후 반드시 조정해야 합니다.

---

## 9. 파일 구조

```text
README.md
  실행, 조작, 배포를 위한 짧은 사용자 문서

PROJECT_HANDOFF.md
  모든 개발 기록, 연결 정보, 테스트, 다음 작업을 누적하는 단일 문서

index.html
  전체 게임 DOM, HUD, 모달, 선택 소환 화면

src/main.js
  Three.js 월드, 전투, 소환, 합성, 궁극기, 웨이브, 적 패턴

src/game-data.js
  도깨비, 적, 등급, 시너지, 축복 정적 데이터

src/sound-engine.js
  Web Audio 절차형 효과음 엔진

src/style.css
  모바일 HUD, 선택 소환, 신화 연출, 전투 피드백 스타일

src/firebase.js
  Firebase 익명 인증과 dokkaebiScores 랭킹

public/sw.js
  PWA 캐시. 패치마다 CACHE 버전 갱신 필요

scripts/verify-project.mjs
  DOM, 버전, 문서 정리, 필수 기능 정적 검사

firestore.rules
  기존 Defense와 새 게임 랭킹 규칙 병합본

.github/workflows/deploy.yml
  main push 시 /Defense/ 경로 GitHub Pages 자동 배포
```

v1.3에서 정적 데이터와 오디오를 먼저 분리했습니다. `src/main.js`에는 Three.js 객체와 서로 강하게 결합된 런타임 로직이 남아 있습니다. 다음 분리 후보는 효과와 전투 텍스트이지만, 게임 기능 개발을 멈추는 대규모 리팩터링은 금지합니다.

---

## 10. 버전 기록

### v1.0.0

- 최초 3D 세로 슬라이스
- 플레이어 직접 이동과 골드 수집
- 도깨비 6종, 자동 합성, 인연 6종
- 적 4종, 보스 2종, 10웨이브
- 로컬/Firebase 랭킹과 PWA

### v1.1.0

- 첫 무료 2성 강림과 첫 웨이브 자동 시작
- 미션 배너, 보스 체력바
- 연속 처치, 치명타, 전투 숫자
- 무결점 보너스, 진동, 적응형 렌더 품질
- 기존 GitHub/Firebase 연결 반영
- 전체 ZIP과 패치 ZIP 전달 체계 확립

### v1.2.0

- 질주꾼 돌진 경고선과 충돌
- 저주 무당 장판 유도
- 돌갑옷 정면 방패
- 보스 충격파 예고 링과 회피 보너스
- 삼지선다 소환권
- 루트 문서 2개로 정리

### v1.3.0

- 5성 도깨비 고유 궁극기 6종
- 5성 탄생 카메라와 신화 각성 연출
- 합성 `1/3`, `2/3` 진행 표시
- 첫 플레이 임무 3단계와 보상 저장
- `game-data.js`, `sound-engine.js` 최소 모듈 분리
- PWA 캐시 버전 v1.3.0 갱신

---

## 11. 검증 상태

2026-07-20 자동 검증 완료:

- `npm ci` 통과
- `npm audit`는 패키지 프록시 502 오류로 이번 환경에서 결과 확인 불가
- `npm run verify` 통과
- 85개 DOM ID 연결 통과
- package/runtime 버전 `1.3.0` 일치
- 루트 Markdown 2개 검사 통과
- JavaScript 문법 검사 통과
- Vite 기본 프로덕션 빌드 통과
- GitHub Pages `/Defense/` base path 별도 빌드 통과

환경 제한:

- 제작 환경 Chromium에서 EGL/ANGLE WebGL 컨텍스트 생성 실패
- 자동 3D 화면 캡처와 실제 터치 플레이 검수 불가

실제 기기 필수 확인:

1. Android Chrome 세로/가로 화면
2. iPhone Safari 안전 영역
3. 붉은 돌진선의 방향과 가시성
4. 저주 장판 예고와 활성 상태 구분
5. 방패 정면/측면 피해 차이
6. 보스 링 회피 판정
7. 삼지선다 모달의 작은 화면 스크롤
8. 웨이브 7 이후 프레임
9. PWA 업데이트 후 v1.3 캐시 적용
10. 5성 탄생 카메라가 조작을 과도하게 방해하지 않는지
11. 5성 2개 이상에서 궁극기 동시 사용 프레임
12. 초행 임무 패널과 조이스틱 겹침

---

## 12. 알려진 위험

1. Firebase Hosting에 배포하면 기존 `web-game2` Hosting 콘텐츠가 교체될 수 있음
2. Firestore 규칙은 반드시 현재 병합 규칙 사용
3. `junl-im/Defense` 저장소는 Pages 주소 기반 추정이므로 GitHub Desktop origin 확인 필요
4. 서비스 워커 캐시 문자열은 패치마다 갱신해야 함
5. 저주 장판과 다수 파티클이 저사양 기기에서 비용이 될 수 있음
6. 돌갑옷 방패 판정이 중앙 배치 도깨비 공격을 많이 차단할 수 있어 난이도 측정 필요
7. 보스 회피 점수 반복 획득량 밸런스 확인 필요
8. 5성 궁극기 다중 발동 시 저사양 기기의 파티클과 라인 비용 확인 필요
9. 신화 카메라 연출 중 보스 경고 링을 놓치지 않도록 시간 감속 체감 확인 필요
10. 초행 임무 완료 키를 삭제하지 않으면 반복 QA에서 패널이 표시되지 않음

---

## 13. 다음 패치 예상 — v1.4.0

가칭: **보스 2페이즈 & 전장 선택 패치**

목표:

- 웨이브 5와 10 보스를 체력 구간에 따라 변하는 전투로 강화
- 궁극기가 강해진 만큼 후반 전투의 회피와 판단 밀도 보강
- 전투 시작 전에 작은 위험/보상 선택을 제공해 판별 변주 확대

예상 작업 우선순위:

1. 저승 호랑이 체력 50% 이하 광폭 돌진 패턴
2. 백귀 야행왕 체력 65%, 30% 단계별 장판과 소환 패턴
3. 보스 패턴 이름과 다음 공격 예고 HUD
4. 웨이브 6 전후 위험/보상 계약 3개 중 하나 선택
5. 5성 궁극기 쿨타임을 보유 목록에 실시간 표시할지 성능 검토
6. 실제 모바일 QA 결과 반영
7. v1.4 전체 ZIP, 패치 ZIP, 체크섬 생성

---

## 14. 다음 작업자가 시작할 순서

1. 이 파일 전체 읽기
2. GitHub Desktop 실제 origin 확인
3. `npm install`
4. `npm run verify`
5. `npm run build`
6. 실제 기기 QA 결과가 있다면 가장 먼저 반영
7. v1.4는 저승 호랑이 2페이즈를 먼저 완성한 뒤 백귀 야행왕으로 확장
8. 패치 완료 후 루트 Markdown이 2개인지 확인
9. 전체 ZIP과 `PATCH_FILES/` 방식 패치 ZIP 생성
10. 패치 적용 비교 테스트와 SHA-256 생성
