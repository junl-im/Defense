import { COMBAT_ART_TEXTURE_IDS, GUARDIAN_CITADEL_STATE_TEXTURE_IDS_V114, P0_DIRECTIONAL_ATLAS_IDS } from '../engine/asset-catalog.js';

export const ART_APPROVAL_PIPELINE_V115 = Object.freeze({
  version: '1.0.15',
  build: 'b24.15',
  id: 'DD-ART-APPROVAL-PIPELINE-1.0',
  statuses: Object.freeze(['approved', 'revise', 'quarantined', 'replacement-pending']),
  gates: Object.freeze([
    'pc-mobile-scale', 'ground-contact', 'silhouette', 'alpha-edge',
    'hit-anchor', 'effect-readability', 'runtime-memory'
  ]),
  approvedRuntime: Object.freeze([
    ...Object.values(COMBAT_ART_TEXTURE_IDS.heroes),
    ...Object.values(COMBAT_ART_TEXTURE_IDS.guardians),
    ...Object.values(COMBAT_ART_TEXTURE_IDS.monsters),
    ...Object.values(COMBAT_ART_TEXTURE_IDS.bosses),
    ...Object.values(GUARDIAN_CITADEL_STATE_TEXTURE_IDS_V114)
  ]),
  quarantined: Object.freeze([
    ...Object.values(P0_DIRECTIONAL_ATLAS_IDS.heroes),
    ...Object.values(P0_DIRECTIONAL_ATLAS_IDS.guardians),
    ...Object.values(P0_DIRECTIONAL_ATLAS_IDS.monsters),
    ...Object.values(P0_DIRECTIONAL_ATLAS_IDS.bosses)
  ]),
  runtimeRules: Object.freeze({
    approvedOnly: true,
    autoGeneratedDirectionalRuntimeAllowed: false,
    brokenImageAltTextAllowed: false,
    firstScreenCriticalOnly: true,
    deferredLoadingRequired: true
  })
});

export function createArtApprovalReportV115() {
  return Object.freeze({
    version: ART_APPROVAL_PIPELINE_V115.version,
    build: ART_APPROVAL_PIPELINE_V115.build,
    approved: ART_APPROVAL_PIPELINE_V115.approvedRuntime.length,
    revise: 0,
    quarantined: ART_APPROVAL_PIPELINE_V115.quarantined.length,
    replacementPending: ART_APPROVAL_PIPELINE_V115.quarantined.length,
    runtimeApplied: ART_APPROVAL_PIPELINE_V115.approvedRuntime.length,
    gates: ART_APPROVAL_PIPELINE_V115.gates
  });
}
