import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const checkOnly = process.argv.includes('--check');
const selfTest = process.argv.includes('--self-test');
const TARGET = Object.freeze({
  releaseVersion: '1.0.52',
  lineageVersion: '23.12.0',
  buildEpoch: 24,
  buildRevision: 52,
  buildId: 'b24.52',
  versionPolicy: 'DD-VERSION-POLICY-1.0',
  cacheRevision: '1.0.52-b24.52'
});
const ALLOWED = new Set(['1.0.51', '1.0.52']);
const stableJson = (value) => `${JSON.stringify(value, null, 2)}\n`;
function outputs(pkg, lock) {
  if (pkg?.name !== 'dokkaebi-luck-defense-3d' || !ALLOWED.has(String(pkg.version || ''))) throw new Error(`v152 identity sync refused for ${pkg?.name || '<missing>'}@${pkg?.version || '<missing>'}`);
  const nextPkg = structuredClone(pkg); nextPkg.version = TARGET.releaseVersion; nextPkg.dokkaebi = { ...(nextPkg.dokkaebi || {}), ...TARGET };
  const nextLock = structuredClone(lock); nextLock.version = TARGET.releaseVersion; nextLock.packages ||= {}; nextLock.packages[''] ||= {}; nextLock.packages[''].version = TARGET.releaseVersion; nextLock.packages[''].dokkaebi = { ...(nextLock.packages[''].dokkaebi || {}), ...TARGET }; nextLock.dokkaebi = { ...(nextLock.dokkaebi || {}), ...TARGET };
  const source = `// Generated by scripts/generate-release-identity-v152.mjs. Do not edit manually.\nexport const RELEASE_IDENTITY = Object.freeze(${JSON.stringify(TARGET, null, 2)});\nexport const RELEASE_VERSION = RELEASE_IDENTITY.releaseVersion;\nexport const LINEAGE_VERSION = RELEASE_IDENTITY.lineageVersion;\nexport const BUILD_EPOCH = RELEASE_IDENTITY.buildEpoch;\nexport const BUILD_REVISION = RELEASE_IDENTITY.buildRevision;\nexport const BUILD_ID = RELEASE_IDENTITY.buildId;\nexport const CACHE_REVISION = RELEASE_IDENTITY.cacheRevision;\n`;
  const browser = `// Generated by scripts/generate-release-identity-v152.mjs. Do not edit manually.\n(() => {\n  const identity = Object.freeze(${JSON.stringify(TARGET, null, 2)});\n  globalThis.__DOKKAEBI_RELEASE_IDENTITY__ = identity;\n})();\n`;
  return new Map([['package.json', stableJson(nextPkg)], ['package-lock.json', stableJson(nextLock)], ['src/release-identity.generated.js', source], ['public/release-identity.generated.js', browser], ['public/version.json', stableJson(TARGET)]]);
}
if (selfTest) {
  const pkg = { name: 'dokkaebi-luck-defense-3d', version: '1.0.51', scripts: { verify: 'x' } };
  const lock = { version: '1.0.51', packages: { '': { version: '1.0.51' } } };
  const files = outputs(pkg, lock);
  if (JSON.parse(files.get('package.json')).version !== '1.0.52' || JSON.parse(files.get('package-lock.json')).packages[''].version !== '1.0.52') throw new Error('v152 identity self-test failed');
  console.log('PASS v1.0.52 identity self-test (1.0.51 -> 1.0.52)');
  process.exit(0);
}
const pkg = JSON.parse(fs.readFileSync(path.join(root, 'package.json'), 'utf8'));
const lock = JSON.parse(fs.readFileSync(path.join(root, 'package-lock.json'), 'utf8'));
const files = outputs(pkg, lock);
const stale = [...files].filter(([file, expected]) => !fs.existsSync(path.join(root, file)) || fs.readFileSync(path.join(root, file), 'utf8') !== expected).map(([file]) => file);
if (checkOnly) {
  if (stale.length) throw new Error(`v152 generated identity is stale: ${stale.join(', ')}`);
  console.log('PASS v1.0.52 generated release identity');
} else {
  for (const [file, content] of files) { fs.mkdirSync(path.dirname(path.join(root, file)), { recursive: true }); fs.writeFileSync(path.join(root, file), content); }
  console.log(JSON.stringify({ id: 'DD-RELEASE-IDENTITY-GENERATOR-V152', releaseVersion: TARGET.releaseVersion, buildId: TARGET.buildId, files: [...files.keys()] }, null, 2));
}
