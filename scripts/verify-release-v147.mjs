import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const json = (file) => JSON.parse(read(file));
const failures = [];
const check = (value, label) => { if (!value) failures.push(label); };
const pkg = json('package.json');
const lock = json('package-lock.json');
const version = json('public/version.json');
const baseline = json('docs/PERFORMANCE_BASELINE_v1.0.45_DIST.json');
const traces = json('docs/qa/device-viewport-traces-v147.json');
const workflow = read('.github/workflows/deploy.yml');
const chain = read('scripts/verify-dist-chain-v140.mjs');
const offline = read('scripts/offline-reconnect-model-v147.mjs');
const continuity = read('scripts/save-continuity-v147.mjs');
const offlineVerifier = read('scripts/verify-offline-reconnect-v147.mjs');
const browser = read('scripts/run-offline-reconnect-v147.mjs');
const fuzz = read('scripts/save-schema-fuzz-v147.mjs');
const ingestion = read('scripts/device-trace-ingestion-v147.mjs');
const evidence = read('scripts/browser-evidence-bundle-v147.mjs');
const promote = read('scripts/promote-v145-dist-baseline-v147.mjs');
const sw = read('public/sw.js');
const patchRevision = Number(String(pkg.version || '').split('.')[2]);
const buildRevision = Number(pkg.dokkaebi?.buildRevision);
check(Number.isInteger(patchRevision) && patchRevision >= 47 && Number.isInteger(buildRevision) && buildRevision >= 47 && pkg.dokkaebi?.buildId === `b24.${buildRevision}` && pkg.dokkaebi?.cacheRevision === `${pkg.version}-b24.${buildRevision}`, 'package identity');
check(lock.version === pkg.version && lock.packages?.['']?.version === pkg.version && lock.packages?.['']?.dokkaebi?.buildId === pkg.dokkaebi?.buildId, 'lock identity');
check(version.releaseVersion === pkg.version && version.buildId === pkg.dokkaebi?.buildId && version.cacheRevision === pkg.dokkaebi?.cacheRevision, 'public identity');
check(offline.includes('DD-OFFLINE-RECONNECT-ASSURANCE-V147') && offline.includes('offline-launch') && offline.includes('mid-wave-reconnect'), 'offline reconnect deterministic contract');
check(offline.includes('compareDurableSaveSnapshotsV147') && !offline.includes('JSON.stringify(saveAfter) === JSON.stringify(saveBefore)'), 'offline save continuity compares durable namespaces instead of volatile raw storage');
const durableSaveBlock = continuity.slice(continuity.indexOf('DURABLE_SAVE_KEYS_V147'), continuity.indexOf('VOLATILE_STORAGE_KEYS_V147'));
const volatileSaveBlock = continuity.slice(continuity.indexOf('VOLATILE_STORAGE_KEYS_V147'), continuity.indexOf('OFFLINE_SAVE_SENTINEL_V147'));
check(durableSaveBlock.includes("'dokkaebi-guardian-growth-v1'") && durableSaveBlock.includes("'dokkaebi-atomic-save-snapshot-v150'") && !durableSaveBlock.includes('dokkaebi-browser-reliability-v19') && !durableSaveBlock.includes('dokkaebi-wave-checkpoint-v18'), 'offline save continuity limits strict equality to durable player namespaces');
check(volatileSaveBlock.includes('dokkaebi-browser-reliability-v19') && volatileSaveBlock.includes('dokkaebi-wave-checkpoint-v18') && volatileSaveBlock.includes('dokkaebi-persistence-journal-v149'), 'offline save continuity classifies boot diagnostics and journals as volatile');
check(continuity.includes("'dokkaebi-guardian-growth-v1'") && continuity.includes("'dokkaebi-atomic-save-snapshot-v150'") && continuity.includes('canonicalStorageValueV147'), 'offline save continuity covers player progress and canonical JSON values');
check(browser.includes('seed-and-snapshot-save-before-offline-launch') && browser.includes('storageBefore') && browser.includes('storageAfter') && browser.includes('saveDiff'), 'v147 browser runner seeds durable save sentinels and reports exact drift');
check(offlineVerifier.includes('volatile diagnostics') && offlineVerifier.includes('dokkaebi-browser-reliability-v19') && offlineVerifier.includes('dokkaebi-guardian-growth-v1'), 'v147 deterministic model rejects durable drift while allowing diagnostic churn');
check(browser.includes('Network.emulateNetworkConditions') && browser.includes('offline: true') && browser.includes('offline: false') && browser.includes('navigator.serviceWorker.getRegistration') && browser.includes('service-worker-activation'), 'controlled network browser scenarios');
check(browser.includes('--disable-background-timer-throttling') && browser.includes('--disable-backgrounding-occluded-windows') && browser.includes('--disable-renderer-backgrounding'), 'v147 browser runner disables background timer and renderer throttling');
check(browser.includes('V147_SERVICE_WORKER_TIMEOUT_MS') && browser.includes('V147_BROWSER_OPERATION_TIMEOUT_MS') && browser.includes('failedPhase') && browser.includes('diagnostics.steps'), 'v147 browser runner has bounded labeled phase diagnostics');
check(!browser.includes('navigator.serviceWorker.ready'), 'v147 browser runner avoids unbounded serviceWorker.ready promise');
check(browser.includes('DOKKAEBI_GET_INSTALL_STATUS') && browser.includes('precache'), 'v147 browser runner reports service-worker precache progress');
check(sw.includes('const INSTALL_SHELL_ASSETS') && sw.includes("'./assets/game.js'") && sw.includes("'./assets/game.css'"), 'v147 service-worker install shell targets deployable Vite assets');
check(sw.includes('PRECACHE_REQUEST_TIMEOUT_MS') && sw.includes('PRECACHE_CONCURRENCY') && sw.includes('new AbortController()'), 'v147 service-worker install has bounded request and cache concurrency');
const installShell = sw.slice(sw.indexOf('const INSTALL_SHELL_ASSETS'), sw.indexOf('const PRECACHE_CONCURRENCY'));
check(!installShell.includes("'./src/"), 'v147 service-worker install excludes non-deployed source modules');
const precacheBlock = sw.slice(sw.indexOf('async function cacheInstallAsset'), sw.indexOf('async function removeOldCaches'));
check(precacheBlock.includes('new Set(INSTALL_SHELL_ASSETS)') && !precacheBlock.includes('SHELL_ASSETS.map'), 'v147 service-worker install does not fetch the historical source ledger');
check(fuzz.includes('DD-SAVE-SCHEMA-FUZZ-V147') && fuzz.includes("'1.0.42'") && fuzz.includes("'1.0.46'") && fuzz.includes('non-idempotent'), 'five-version save fuzz contract');
check(ingestion.includes('DD-DEVICE-TRACE-INGESTION-V147') && ingestion.includes('SENSITIVE_KEYS') && ingestion.includes('sourceSha256'), 'trace identifier stripping and provenance contract');
check(traces.id === 'DD-DEVICE-TRACE-INGESTION-V147' && traces.traces?.length === 3 && /^[a-f0-9]{64}$/.test(traces.provenance?.sourceSha256 || ''), 'committed trace provenance');
check(evidence.includes('Passed scenarios retain compact summaries') && evidence.includes('failed-full-reports') === false && evidence.includes('chromiumStderrTail'), 'compact pass and diagnostic failure evidence model');
check(promote.includes('buildApprovedBaselineV147') && promote.includes('--candidate') && baseline.id === 'DD-PERFORMANCE-BASELINE-V145-DIST', 'exact baseline promotion tooling');
check(['awaiting-first-approved-vite-artifact','approved-exact-vite-artifact'].includes(baseline.measurementStatus), 'baseline status');
check(sw.includes(`const RELEASE_VERSION = '${pkg.version}'`) && sw.includes(`const BUILD_ID = '${pkg.dokkaebi?.buildId}'`) && sw.includes('DD-SW-UPGRADE-ASSURANCE-V147'), 'service worker identity');
check(chain.includes("'147'") && chain.includes(`v${pkg.version}`), 'dist chain includes v147');
check(workflow.includes('REQUIRE_BROWSER_V147: 1') && workflow.includes('logs/qa/v147') && workflow.includes('v1.0.47-offline-evidence'), 'CI v147 evidence contract');
for (const name of ['verify:baseline:v147','promote:baseline:v147','verify:offline-model:v147','verify:save-fuzz:v147','verify:trace-ingest:v147','verify:device-traces:v147','verify:evidence:v147','verify:browser:v147','compact:evidence:v147','verify:release:v147','verify:dist:v147','verify:trend:v147','stage:package:v147','verify:package:v147','create:patch:v147','verify:patch:v147']) check(Boolean(pkg.scripts?.[name]), `package script ${name}`);
for (const file of ['docs/RELEASE_ASSURANCE_v1.0.47.md','docs/PATCH_NOTES_v1.0.47.md','docs/PATCH_APPLY_v1.0.47.md','docs/NEXT_UPDATE_v1.0.48.md']) check(fs.existsSync(path.join(root, file)), `document ${file}`);
if (failures.length) { failures.forEach((failure) => console.error(`FAIL ${failure}`)); process.exit(1); }
console.log('PASS v1.0.47 offline/reconnect, save fuzz, trace provenance, compact evidence, and baseline promotion contracts');
