KingdomSeed v2.36.9 Prestige Visual Lift Patch

Apply by extracting this zip over the existing KingdomSeed project root.

This patch intentionally excludes:
- .git
- node_modules
- dist
- temporary files

After applying:
1. npm ci
2. npm run build
3. npm run dev

Recommended visual checks:
- default URL: mobile-safe premium fallback actors should look heavier and less toy-like.
- ?quality=low: confirms the static grade and fallback silhouettes remain cheap.
- ?iconmock or ?stickerart: confirms isolated icon QA modes are still separated from the default battle.
- ?flatbattle: confirms the prestige grade can be disabled for comparison/debug.
