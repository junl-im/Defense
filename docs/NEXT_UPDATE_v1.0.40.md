# Next Update v1.0.40

- Run the complete legacy dist gate sequence against an automatically generated Vite fixture in CI.
- Add a bundle size, chunk count, and initial-load budget.
- Build the dynamic JSON manifest-aware runtime asset reachability graph.
- Quarantine unused legacy asset generations before permanent deletion.
- Add browser automation for 100-wave FPS, memory, and WebGL resource recovery.
- Add package size ceilings that reject `dist`, `node_modules`, and duplicated assets in source archives.
