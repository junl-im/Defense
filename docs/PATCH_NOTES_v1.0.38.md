# Patch Notes v1.0.38

- Fixed `verify:dist:v123` false failure after a successful Vite build.
- Limited legacy-title checks to HTML and PWA manifest presentation surfaces.
- Preserved runtime title correction literals and marker.
- Added a synthetic regression fixture for the exact GitHub Actions failure.
- Made the v1.0.37 release and dist foundation gates forward-compatible with v1.0.38.
- Added `verify:release:v138` and `verify:dist:v138` to GitHub Actions.
