# v1.0.33 패치 적용

## Windows

1. 패치 ZIP을 압축 해제한다.
2. 기존 v1.0.32 프로젝트 폴더를 `APPLY_PATCH_WINDOWS.bat` 위로 끌어놓는다.
3. 패치 도구가 기존 파일을 `logs/backups/`에 백업한다.
4. 파일별 SHA-256 검사 후 루트의 과거 컴팩트 파일 2개를 정리한다.

수동 덮어쓰기는 루트의 과거 파일을 삭제하지 못하므로 권장하지 않는다.
