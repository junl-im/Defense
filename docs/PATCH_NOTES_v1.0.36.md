# Patch Notes v1.0.36

- Added exact duplicate measurement between `public/` and generated `dist/`.
- Added a clean source-package staging workflow.
- Excluded generated output and local dependencies from the full source ZIP.
- Added a safe cleanup command for `dist/` and an empty `node_modules/` directory.
- Preserved all runtime assets, production data, v1.0.35 runtime fixes, and mandatory handoff history.
