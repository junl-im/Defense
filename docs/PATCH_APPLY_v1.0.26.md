# v1.0.26 패치 적용 안내

기준 버전은 v1.0.25 / b24.25입니다.

1. 실행 중인 개발 서버와 게임 브라우저를 종료합니다.
2. 프로젝트 전체를 백업합니다.
3. 직접 붙여넣기 패치의 모든 파일과 숨김 폴더 `.github`를 프로젝트 루트에 덮어씁니다.
4. `npm run verify`를 실행합니다.
5. `npm run build` 후 `npm run verify:dist:v126`을 실행합니다.

원클릭 패치는 프로젝트 폴더를 Windows 배치 파일 위로 드래그하여 적용합니다.
