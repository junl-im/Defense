# Next Update v1.0.39

- Audit all remaining historical `verify:dist` scripts for whole-bundle text assertions.
- Build a manifest-aware runtime asset reachability graph.
- Quarantine provably unreachable legacy asset generations before deletion.
- Add bundle size, chunk count, initial load, and source package size budgets to CI.
- Add browser-run 100-wave FPS, memory, WebGL resource, background-resume, and rotation tests.
