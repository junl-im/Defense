# Next Update v1.0.46

- Add real-device trace import and replay for iOS Safari standalone PWA resume, Android address-bar collapse, and virtual-keyboard viewport sequences.
- Persist approved v1.0.45 Vite dist measurements as the first measured bundle trend baseline instead of relying only on the approved source-package baseline and absolute v1.0.44 envelope.
- Add service-worker upgrade simulation across two release caches and verify save continuity during controller replacement.
- Add deterministic combat-load phases to the long-session harness so selected waves retain real enemies, projectiles, particles, boss telegraphs, and reward UI for fixed observation windows.
- Export machine-readable failure digests that identify the first regressing sample and the responsible metric slope.
