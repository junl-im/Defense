# v1.0.27 적용 안내

기준 버전은 v1.0.26입니다.

## 직접 붙여넣기

직접 패치 ZIP의 내용을 프로젝트 최상위 폴더에 덮어쓴다. 숨김 폴더 `.github`도 포함한다.

## 원클릭

압축 해제 후 기존 프로젝트 폴더를 `APPLY_PATCH_WINDOWS.bat` 위로 드래그한다. 변경 파일은 `logs/backups/v1.0.27_날짜_시간`에 백업한다.

## 적용 후

GitHub Actions를 다시 실행하고 `verify:dist:v127` 통과 여부를 확인한다.
