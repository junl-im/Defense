# v7.0.0 overwrite patch

Base version: `DokkaebiLuckDefense3D_FULL_v6.0.0`

1. Back up the base project.
2. Extract the overwrite ZIP into the project root.
3. Allow replacement of files with matching paths.
4. Run `npm run verify`.
5. Run `npm run build:static`.
6. Run `node scripts/verify-static-dist.mjs`.
