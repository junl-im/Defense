# Patch Notes

## Buildfix

- `BossPatternState.ts`가 호출하는 `Tower.applySuppression()`을 `Tower.ts`에 복구했습니다.
- 보스 패턴의 타워 억압 효과가 피해량 감소/공격속도 지연으로 실제 반영됩니다.
- 억압 오라와 짧은 상태 텍스트를 추가했습니다.
- 이번 패치에는 `README-v4.x-...md` 같은 버전별 루트 문서를 넣지 않았습니다.
