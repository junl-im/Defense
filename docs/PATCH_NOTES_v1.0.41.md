# Patch Notes v1.0.41

- Fixed full-map floor tap movement on mobile and desktop pointer input.
- Fixed map touch being permanently disabled by an unsynchronized `body.playing` CSS contract.
- Added pointer-capture loss recovery and global pointer completion handlers.
- Removed the obsolete purple logo loading screen from initial boot and title-to-battle entry.
- Kept the current boot gate and title screen as the only visible startup presentation.
- Bumped offline cache revision so previously cached startup HTML is replaced.
