# Patch Notes v23.0.2 — Clean Foundation

- 루트 파일 허용 목록 강제
- `logs/` 생성 산출물 표준화
- 시뮬레이션과 자동 감사의 기본 출력 위치를 `logs/`로 이동
- `docs/` 기준선은 `--refresh-baseline`에서만 갱신
- CI 검증·빌드 로그를 `logs/`로 이동
- 패치 관리 파일을 `logs/patch/<version>/`에 배치하는 영구 규칙 추가
- 루트 정리 도구와 교차 플랫폼 로그 실행기 추가
