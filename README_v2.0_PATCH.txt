Kingdom Seed Defense v2.0.0 Patch

Apply:
1. Extract this zip over your current project root.
2. Run: npm install
3. Run: npm run build
4. Run: npm run dev

QA:
- Mobile portrait should no longer render as a tiny strip.
- Use ?hit=1 on the dev URL to inspect transparent click zones.
- Login/main/worldmap/combat art now points to v2.0 assets.
