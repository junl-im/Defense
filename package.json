{
  "name": "dokkaebi-luck-defense-3d",
  "private": true,
  "version": "1.7.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "verify": "node scripts/verify-project.mjs && node --check src/main.js && node --check src/game-data.js && node --check src/sound-engine.js && node --check src/firebase.js && node --check src/engine/index.js && node --check src/engine/mobile-engine.js && node scripts/verify-engine-budget.mjs"
  },
  "dependencies": {
    "firebase": "12.16.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
